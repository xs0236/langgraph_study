# 第三阶段：企业级实战 — 并行、子图、Stream、内容工厂

> 🕐 **时间：** 第8-11周
> 🎯 **目标：** 掌握 Send 并行、Subgraph 子图嵌套、Stream 实时输出、综合构建内容工厂 V1
> 📁 **对应代码：** `test/multi_agent_Send.py` → `multi_agent_Supervisor.py`

---

## 第8周：并行执行 — Send 机制

### 🤔 为什么需要并行？

```
# 串行（现在的模式）
调研(技术角度) → 调研(教育角度) → 调研(伦理角度) → 汇总
# 总共耗时 = 3次 LLM 调用时间相加

# 并行（Send 模式）
[调研(技术角度), 调研(教育角度), 调研(伦理角度)] → 汇总
# 总共耗时 = 最慢的那次 LLM 调用时间
```

### 📮 Send 是什么？

**Send = "把同一个任务用不同参数，同时执行 N 次"**

```python
from langgraph.types import Send

def supervisor_node(state):
    """不再返回 dict，而是返回 Send 列表"""
    return [
        Send("researcher", {"topic": state["topic"], "angle": "技术角度"}),
        Send("researcher", {"topic": state["topic"], "angle": "教育角度"}),
        Send("researcher", {"topic": state["topic"], "angle": "伦理角度"}),
    ]
    # ★ 关键：返回的是列表，不是字典！
    # 同一个 researcher 节点，会同时启动 3 个副本
    # 每个副本收到不同的参数（不同 angle）
```

### 🧩 Send 执行流程

```
Send("researcher", {"angle": "技术角度"})    → 启动副本1
Send("researcher", {"angle": "教育角度"})    → 启动副本2    ← 同时执行！
Send("researcher", {"angle": "伦理角度"})    → 启动副本3
                                                    ↓
                                          [副本1完成, 副本2完成, 副本3完成]
                                                    ↓
                                              汇总节点（Aggregator）
```

### ⚠️ 核心限制：Send 只能在条件边中使用

这是 LangGraph 的一个**非常重要**的限制：

```python
# ❌ 错误：Send 在普通节点中返回
def trans_fan_out(state):
    # 普通节点必须返回 dict！
    return [Send("worker", {"lang": "en", ...})]  # ← 报错！

# ✅ 正确：Send 在条件边函数中返回
def trans_fan_out_mapper(state):
    # 条件边函数可以返回 Send 列表！
    return [Send("worker", {"lang": "en", ...})]

# 边的定义方式也不同：
# 普通节点：
mg.add_node("trans_fan_out", trans_fan_out)  # 函数当节点
mg.add_edge("trans_fan_out", "next_node")     # 普通边

# 条件边（Send）：
# trans_fan_out_mapper 不作为节点，只作为条件边函数
mg.add_conditional_edges(
    "coordinator",              # 从 coordinatior 节点出发
    trans_fan_out_mapper,       # 条件边函数（返回 Send 列表）
    ["translate_one"]           # Send 的目标节点
)
```

### 🧩 完整的并行模式

```python
from langgraph.graph import StateGraph, START, END
from langgraph.types import Send
from typing import Annotated, TypedDict

class State(TypedDict):
    topic: str
    partial_notes: list  # 收集多个调研结果
    final_notes: str

# ===== 1. 并行工作节点 =====
def researcher_worker(state):
    """调研工人：根据角度做调研"""
    angle = state.get("angle", "综合")
    topic = state.get("topic", "")
    result = model.invoke([HumanMessage(content=f"从{angle}调研{topic}")])
    return {"partial_notes": [f"【{angle}】{result.content}"]}
    # 注意：add_messages 机制对普通字段不生效
    # 需要在 State 中特殊处理 partial_notes 的追加

# ===== 2. 汇总节点 =====
def aggregator(state):
    """把多个调研结果汇总成一份"""
    all_notes = "\n\n".join(state.get("partial_notes", []))
    return {"final_notes": all_notes}

# ===== 3. 条件边函数（返回 Send）=====
def fan_out_researchers(state):
    """返回 Send 列表，并行派发多个调研任务"""
    return [
        Send("researcher", {"topic": state["topic"], "angle": "技术角度"}),
        Send("researcher", {"topic": state["topic"], "angle": "教育角度"}),
        Send("researcher", {"topic": state["topic"], "angle": "伦理角度"}),
    ]

# ===== 4. 构建图 =====
builder = StateGraph(State)

# 注册节点
builder.add_node("start_node", lambda s: {})  # 空节点，作为入口
builder.add_node("researcher", researcher_worker)
builder.add_node("aggregator", aggregator)

# 入口
builder.set_entry_point("start_node")

# 条件边：从 start_node 出发，并行派发到 researcher
builder.add_conditional_edges("start_node", fan_out_researchers, ["researcher"])
# ★ 注意：条件边直接映射到 target 节点列表，不是字典！

# researcher 完成 → 到 aggregator
builder.add_edge("researcher", "aggregator")
builder.add_edge("aggregator", END)
```

### 💡 第8周任务

1. 运行 `test/multi_agent_Send.py`，观察并行执行
2. **理解**：为什么 Send 必须在条件边中？为什么不能在普通节点返回？
3. 修改代码：让翻译也变成并行（英文和日文同时翻译）

---

## 第9周：子图嵌套（Subgraph）

### 🤔 为什么需要子图？

```
# 不封装：所有节点堆在一起
main_graph:
  supervisor → researcher → writer → editor → translator → publisher
  # 主图越来越臃肿，难以维护

# 封装后：模块化管理
main_graph:
  supervisor → [写作子图] → [翻译子图] → [发布子图] → END
  
  写作子图内部：
    supervisor → researcher → writer → editor
```

**子图的本质：把一组节点打包成"黑盒"，主图只关心它的输入和输出。**

### 🧩 子图如何定义

```python
# ===== 子图内部（独立的 StateGraph）=====
def build_writing_subgraph():
    """构建写作子图"""
    
    # 子图自己的状态（可以和主图字段重叠）
    class WritingState(TypedDict):
        topic: str
        research_notes: str
        draft: str
        edit_feedback: str
        next_agent: str
    
    # 子图内部的节点和边
    sg = StateGraph(WritingState)
    sg.add_node("supervisor", w_supervisor)
    sg.add_node("researcher", w_researcher)
    sg.add_node("writer", w_writer)
    sg.add_node("editor", w_editor)
    
    sg.set_entry_point("supervisor")
    sg.add_conditional_edges("supervisor", ...)
    sg.add_edge("researcher", "supervisor")
    sg.add_edge("writer", "supervisor")
    sg.add_edge("editor", "supervisor")
    
    return sg.compile()  # ← 返回编译好的子图

# ===== 主图使用子图 =====
main_graph = StateGraph(MainState)

# 子图作为一个节点加入主图！
main_graph.add_node("writing_team", build_writing_subgraph())
# writing_team 在主图看来就是一个普通节点
# 但它内部包含了完整的 Researcher → Writer → Editor 流程

main_graph.add_edge("writing_team", "main_supervisor")
```

### 🔄 子图执行流程

```
主图 Supervisor: "下一步去 writing_team"
                     ↓
            ┌─ 进入 Writing Subgraph ─┐
            │  Researcher → Writer →  │  ← 子图内部自己流转
            │  → Editor → FINISH      │
            └──────────┬──────────────┘
                       ↓
            子图输出（draft, edit_feedback）
                       ↓
            合并到主图 State
                       ↓
            主图 Supervisor 继续调度
```

### ⚠️ 关键陷阱：字段名必须一致

```python
# ❌ 错误：子图输出 draft，主图查 article
# 子图写入: {"draft": "文章内容..."}
# 主图读取: state.get("article")  → None！

# ✅ 正确：字段名必须完全一致
# 子图写入: {"article": "文章内容..."}
# 主图读取: state.get("article")  → 拿到！
```

**原因：** LangGraph 按字段名把子图的输出合并到主图状态。字段名不同=数据没传过去。

### 💡 第9周任务

1. 运行 `test/multi_agent_Subgraphs.py`
2. 分析：哪些节点在子图里，哪些在主图里？
3. 实验：故意把子图字段名写错，观察会发生什么

---

## 第10周：Stream 实时输出

### 🤔 为什么需要 Stream？

```python
# invoke（阻塞等待）
result = app.invoke(inputs)
# 用户等了 30 秒，什么反馈都没有...
# 心里想："是不是卡死了？"

# stream（实时输出）
for chunk in app.stream(inputs):
    print(chunk)
# 每个节点完成就打印：
# {"supervisor": {"next_agent": "researcher"}}
# {"researcher": {"research_notes": "调研笔记..."}}
# {"writer": {"draft": "文章初稿..."}}
```

### 📡 Stream 数据结构

```python
for chunk in app.stream(inputs, config):
    # chunk 是一个字典：{节点名: {状态更新}}
    
    # 比如：
    # {"m_s": {"next_agent": "writing"}}
    # {"writing": {"article": "文章..."}}
    # {"trans": {"english_version": "English text..."}}
    
    for node_name, node_output in chunk.items():
        if node_name == "__interrupt__":
            # 遇到人工审核中断
            payload = node_output[0].value
            print(f"⏸️ 暂停：{payload['message']}")
        else:
            print(f"📡 节点 {node_name} 完成")
```

### 🚀 配合 FastAPI 做 Web 应用

```python
from fastapi import FastAPI
from fastapi.responses import StreamingResponse
import json

app = FastAPI()

@app.post("/generate")
async def generate(topic: str):
    async def event_generator():
        config = {"configurable": {"thread_id": "web_001"}}
        inputs = {"topic": topic, "messages": [("user", topic)]}
        
        for chunk in graph.stream(inputs, config):
            for node_name in chunk.keys():
                # SSE 格式：data: {...}\n\n
                yield f"data: {json.dumps({'node': node_name})}\n\n"
        
        yield "data: [DONE]\n\n"
    
    return StreamingResponse(
        event_generator(),
        media_type="text/event-stream"
    )
```

### 💡 第10周任务

1. 运行 `test/multi_agent_Stream.py`
2. 对比 invoke 和 stream 的输出区别
3. 写出一个能区分"普通节点"和"中断"的 stream 循环

---

## 第11周：内容工厂 V1 — 综合实战

### 🏗️ 架构设计

```
┌──────────────────────────────────────────────────────────────┐
│                      主图 (Main Graph)                        │
│                                                              │
│  Supervisor → 写作子图 → 翻译子图 → 配图子图 → 发布子图 → END │
└──────────────────────────────────────────────────────────────┘
     ↓              ↓              ↓              ↓
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│ 写作子图  │  │ 翻译子图  │  │ 配图子图  │  │ 发布子图  │
│ 调研→撰稿 │  │ 中→英    │  │ 生成     │  │ 微信     │
│ →编辑     │  │ 中→日    │  │ 提示词   │  │ 知乎     │
└──────────┘  └──────────┘  └──────────┘  │ 微博     │
                                          └──────────┘
```

### 🧩 主图调度逻辑

```python
def main_supervisor(state):
    """主调度器：按阶段顺序调度子图"""
    
    # 阶段1：写作
    if not state.get("article"):
        return {"next_agent": "writing"}
    
    # 阶段2：翻译
    if not state.get("english_version") or not state.get("japanese_version"):
        return {"next_agent": "trans"}
    
    # 阶段3：配图
    if not state.get("image_url"):
        return {"next_agent": "image"}
    
    # 阶段4：发布
    if not state.get("wechat_url") or not state.get("zhihu_url"):
        return {"next_agent": "publish"}
    
    # 全部完成
    return {"next_agent": "FINISH"}
```

### 🧩 4个子图各司其职

```python
# 每个子图都是一个独立的编译图

# 1. 写作子图
writing_sg = build_writing_subgraph()
# 输入：topic
# 输出：article, edit_feedback

# 2. 翻译子图  
trans_sg = build_translation_subgraph()
# 输入：article
# 输出：english_version, japanese_version

# 3. 配图子图
image_sg = build_image_subgraph()
# 输入：topic, article
# 输出：image_prompt, image_url

# 4. 发布子图
publish_sg = build_publish_subgraph()
# 输入：article, topic
# 输出：wechat_url, zhihu_url, weibo_text
```

### 📁 文件参考

`test/multi_agent_Supervisor.py` 实现了完整的 4 子图嵌套架构。

### 💡 第11周任务

1. 运行 `test/multi_agent_Supervisor.py`，观察完整流程
2. **理解每个子图的职责边界**
3. 分析：为什么主图调度用 if-else 就够了？什么情况下需要用 LLM 调度？

---

## 🏆 里程碑考核：内容工厂 V1

### 要求

实现一个内容工厂，输入一个主题，自动完成：
1. **写作子图：** 调研 → 撰稿 → 编辑（子图封装）
2. **翻译子图：** 中文 → 英文、中文 → 日文（子图封装）
3. **配图子图：** 生成配图提示词（子图封装）
4. **发布子图：** 生成各平台文案（子图封装）
5. **主调度：** 按阶段顺序调度

### 验收清单

| 能力 | 通过标准 |
|------|---------|
| 子图封装 | 4 个子图各自独立定义，不互相干扰 |
| 状态穿透 | 子图的输出能正确合并到主图状态 |
| 主图调度 | 能按阶段依次调度各个子图 |
| 完整流程 | 一个 topic 输入，最终输出 6 份成果 |
| 代码理解 | 能解释为什么字段名必须一致 |

### 输出预览

```
主题: "人工智能对现代教育的影响"

产出:
📝 文章正文（中文）
🇺🇸 英文翻译
🇯🇵 日文翻译
🎨 配图提示词
📱 公众号文案
💡 知乎文案
🔥 微博文案
```

---

## 🚀 第三阶段总结

### 你学到的核心概念

```
✅ Send              — 并行派发任务
✅ Subgraph          — 图中图（模块化）
✅ Stream            — 实时输出
✅ Aggregator        — 并行结果汇总
✅ 主图+子图架构    — 分层设计
✅ 状态穿透          — 子图输出合并到主图
```

### 你的能力成长

| 阶段 | 你能做什么 | 相当于什么水平 |
|------|-----------|--------------|
| 第一阶段 | 单 Agent | 实习 |
| 第二阶段 | 多 Agent 团队 | 初级 |
| 第三阶段 | 企业级流水线 | 中级 |

### 下一步

你已经掌握了 LangGraph 的核心架构能力。第四阶段将把内容工厂从"能跑"变成"能用"。

> 📖 打开 `第四阶段-产品化与面试准备.md` 继续学习
