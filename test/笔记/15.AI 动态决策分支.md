# 代码文件 content_factory_ai_decision.py

核心改动：AI 动态决策分支

1. 新增三个 Pydantic 结构化输出模型
   Python
   class QualityCheck(BaseModel):
   score: int = Field(description="文章质量评分 1-10")
   issues: List[str] = Field(description="存在的问题列表")
   should_rewrite: bool = Field(description="是否需要重写")

class ImageDecision(BaseModel):
need_image: bool = Field(description="这篇文章是否需要配图")
reason: str = Field(description="判断理由")

class PublishPlan(BaseModel):
platforms: List[str] = Field(description="选择要发布的平台")
wechat_style: str = Field(description="微信公众号文案风格建议")
zhihu_style: str = Field(description="知乎文案风格建议")
weibo_style: str = Field(description="微博文案风格建议") 2. 新增 AI 决策节点
表格
节点 功能 效果
ai_quality_check AI 给文章打分 1-10 低于 7 分自动重写，无需人工干预
ai_image_decision AI 判断是否需要配图 技术教程→配图；短评→跳过
ai_publish_plan AI 选择最适合的平台 深度长文→微信+知乎；热点→微博3. 新流程顺序
plain
写作子图 → AI自检(ai_quality_check) → 人工审核(human_review)
↓ (评分<7自动回写作)
翻译(Send并行) → AI配图决策(ai_image_decision) → [可选]配图
↓
AI发布计划(ai_publish_plan) → 发布(Send并行，只发AI选中的平台) 4. 主调度关键逻辑
Python

# AI 自检评分低于 7 分 → 自动重写，不打扰用户

if s.get("ai_quality_score", 10) < 7 and s.get("human_decision") != "continue":
return {"next_agent": "writing", ...}

# AI 判断不需要配图 → 跳过配图节点

if s.get("ai_need_image") is None:
return {"next_agent": "ai_image_decision"}
if not s.get("ai_need_image"): # 直接跳过配图，进入发布计划 5. 发布节点根据 AI 决策动态派发
Python
def pub_fan_out(state):
platforms_str = state.get("ai_publish_platforms", "wechat,zhihu,weibo")
platforms = [p.strip() for p in platforms_str.split(",") if p.strip()] # 只派发 AI 选中的平台
if "wechat" in platforms: sends.append(Send("pub_wechat", ...))
if "zhihu" in platforms: sends.append(Send("pub_zhihu", ...))
if "weibo" in platforms: sends.append(Send("pub_weibo", ...))
运行
bash
python content_factory_ai_decision.py
预期效果：
文章写完后，AI 先自检评分，如果低于 7 分会自动重写
自检通过后进入人工审核，此时进度会显示 AI 评分
翻译后 AI 判断是否需要配图，技术类文章可能需要，短评可能跳过
发布前 AI 制定计划，可能只选 2 个平台而不是固定 3 个

下一步方向清单（这次多列举）
表格
方向 核心能力 实战场景 难度
C. 错误处理与重试 节点失败自动重试、走 fallback、超时控制 API 限流/网络抖动时自动重试 3 次，失败转人工 ⭐⭐
B. 持久化记忆 Redis/MongoDB 存 checkpoint，程序重启状态不丢 用户今天写到一半，明天打开网页继续 ⭐⭐
H. Web Search / RAG 写作子图联网查资料，不只是靠模型知识 写"最新 AI 趋势"时自动搜索 2026 年数据 ⭐⭐⭐
A. Map-Reduce 长任务拆成多个小任务并行，再聚合 1万字文章拆成5段并行润色 ⭐⭐⭐
F. LangSmith 监控 追踪每次执行的耗时、token、调用链 生产环境排查"哪个节点最慢、最费钱" ⭐⭐
D. 多智能体协作 多个独立 Agent（写作/翻译/审核）各自有记忆 每个 Agent 有自己的"人格"和长期记忆 ⭐⭐⭐⭐
G. 动态图构建 运行时根据用户输入动态增减节点 用户勾选"只需要翻译"，图自动精简 ⭐⭐⭐
I. 异步事件驱动 外部系统完成回调后触发图继续 配图节点调用 Stable Diffusion API，生成完成后 webhook 回调继续 ⭐⭐⭐⭐
推荐顺序：
C（错误处理） — 你现在代码里没有任何 try/except，API 一报错就崩，这个最实用
H（Web Search） — 让文章质量飞跃，从"模型瞎编"变成"基于真实数据"
B（持久化记忆） — 做成真正可用的产品必备
