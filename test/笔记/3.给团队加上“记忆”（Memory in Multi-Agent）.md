<!-- 7.13 -->

# 代码文件 multi_agent_with_memory.py

# 方向一：给团队加上“记忆”（Memory in Multi-Agent）

太棒了！给多 Agent 团队加上记忆，是迈向“生产级”应用的关键一步。
🧠 核心原理：团队是如何“记住”事情的？
在单 Agent 中，我们只需要记住 messages（对话历史）。但在多 Agent 中，我们不仅要记住对话，还要记住“团队的共享白板”（State）。
LangGraph 的 Checkpointer 非常聪明：当你传入 Checkpointer 时，它会自动把整个 State（包括 topic、research_notes、draft 等所有字段）序列化并保存到内存中。只要 thread_id 相同，下次唤醒团队时，白板上的所有数据都会原封不动地恢复！
💻 实战：让团队拥有“跨轮次记忆”
我们需要做两个关键修改：
引入 MemorySaver 并在编译时传入。
修改运行逻辑，支持“多轮对话”（使用 app.invoke 代替 app.stream 会更清晰）。
你可以新建一个文件 multi_agent_with_memory.py，运行以下完整代码：
python

编辑

import os
from dotenv import load_dotenv
from typing import Annotated
from typing_extensions import TypedDict
from langgraph.graph import StateGraph, END
from langgraph.graph.message import add_messages
from langgraph.checkpoint.memory import MemorySaver
from langchain_openai import ChatOpenAI
from langchain_core.messages import HumanMessage

load_dotenv()

### 1. 初始化模型

model = ChatOpenAI(
model="glm-4-flash",
openai_api_base="https://open.bigmodel.cn/api/paas/v4/",
temperature=0
)

### 2. 定义团队共享状态 (Shared State)

class TeamState(TypedDict):
messages: Annotated[list, add_messages]
topic: str  
 research_notes: str  
 draft: str  
 edit_feedback: str  
 next_agent: str

### 3. 定义专业 Agent 节点 (与之前相同)

def researcher_node(state: TeamState):
print("🕵️ [Researcher] 正在搜集资料...")
prompt = f"你是一位资深调研员。请针对主题 '{state['topic']}' 搜集关键信息，输出简短的调研笔记。"
response = model.invoke([HumanMessage(content=prompt)])
return {"research_notes": response.content, "next_agent": "supervisor"}

def writer_node(state: TeamState):
print("✍️ [Writer] 正在撰写初稿...")
prompt = f"你是一位资深撰稿人。请根据以下调研笔记撰写短文初稿：\n{state['research_notes']}"
response = model.invoke([HumanMessage(content=prompt)])
return {"draft": response.content, "next_agent": "supervisor"}

def editor_node(state: TeamState):
print("📝 [Editor] 正在审核文章...")
prompt = f"你是一位严格的编辑。请审核以下初稿并给出修改意见：\n{state['draft']}"
response = model.invoke([HumanMessage(content=prompt)])
return {"edit_feedback": response.content, "next_agent": "FINISH"}

### 4. 定义主管节点 (Supervisor)

def supervisor_node(state: TeamState):
print("👔 [Supervisor] 正在思考下一步...")
if not state.get("research_notes"):
next_agent = "researcher"
elif not state.get("draft"):
next_agent = "writer"
elif not state.get("edit_feedback"):
next_agent = "editor"
else:
next_agent = "FINISH"
return {"next_agent": next_agent}

### 5. 构建图 (Graph)

workflow = StateGraph(TeamState)
workflow.add_node("supervisor", supervisor_node)
workflow.add_node("researcher", researcher_node)
workflow.add_node("writer", writer_node)
workflow.add_node("editor", editor_node)
workflow.set_entry_point("supervisor")

workflow.add_conditional_edges(
"supervisor",
lambda state: state["next_agent"],
{"researcher": "researcher", "writer": "writer", "editor": "editor", "FINISH": END}
)
workflow.add_edge("researcher", "supervisor")
workflow.add_edge("writer", "supervisor")
workflow.add_edge("editor", "supervisor")

### 6. 【核心修改】：引入记忆并编译

memory = MemorySaver()
app = workflow.compile(checkpointer=memory)

### 7. 运行多轮对话测试

config = {"configurable": {"thread_id": "team_001"}}

#### === 第一轮对话：启动团队写文章 ===

print("\n--- 第一轮对话：启动团队 ---")
inputs1 = {"topic": "人工智能对现代教育的影响", "messages": [("user", "帮我写一篇关于AI教育的文章")]}
final_state_1 = app.invoke(inputs1, config=config)
print(f"🎉 第一轮结束！文章初稿已完成。")

#### === 第二轮对话：基于上一轮的草稿继续工作 ===

print("\n--- 第二轮对话：追加任务（测试记忆） ---")

### 注意：这里不需要再传 topic，也不需要重置状态

### 只需要传入新的消息，Agent 会自动从 memory 中恢复之前的白板！

inputs2 = {"messages": [("user", "请把刚才那篇文章的标题改得更吸引人一点，并重新生成一版")]}
final_state_2 = app.invoke(inputs2, config=config)

print("\n--- 最终结果展示 ---")
print(f"最终文章初稿:\n{final_state_2['draft']}")
print(f"编辑最终意见:\n{final_state_2['edit_feedback']}")
💡 运行后观察重点
第一轮：团队会像往常一样，经历 Researcher -> Writer -> Editor 的完整流水线。
第二轮：当你输入“请把刚才那篇文章的标题改得更吸引人一点”时，你会神奇地发现：
主管不会再派 Researcher 去查资料（因为 research_notes 还在！）。
主管会直接派 Writer 去修改（因为 draft 还在，但我们可以稍微修改一下 Prompt 让 Writer 知道要重写）。
数据隔离：如果你把 thread_id 改成 team_002，团队就会彻底忘记之前的事情，从零开始。这就是多租户（Multi-tenant）系统的雏形！

#### 运行结果

--- 第一轮对话：启动团队 ---
👔 [Supervisor] 正在思考下一步...
🕵️ [Researcher] 正在搜集资料...
👔 [Supervisor] 正在思考下一步...
✍️ [Writer] 正在撰写初稿...
👔 [Supervisor] 正在思考下一步...
📝 [Editor] 正在审核文章...
👔 [Supervisor] 正在思考下一步...
🎉 第一轮结束！文章初稿已完成。

--- 第二轮对话：追加任务（测试记忆） ---
👔 [Supervisor] 正在思考下一步...

--- 最终结果展示 ---
最终文章初稿:
标题：人工智能在现代教育中的双刃剑效应与未来展望

随着科技的飞速发展，人工智能（AI）已经渗透到我们生活的方方面面，教育领域也不例外。AI技术的应用对现代教育产生了深远的影响，既有积极的一面，也存在一些潜在的挑战。

一、AI教育的正面影响

1. 效率提升：AI技术能够自动批改作业，减轻教师的批改负担，同时提供个性化的学习方案，使教学过程更加高效。

2. 个性化学习：AI系统可以根据学生的特点，推荐适合的学习路径和资源，从而提高学习效果，满足不同学生的学习需求。

3. 智能辅导：AI辅助教学，能够实时解答学生的疑问，提供针对性的辅导，帮助学生更好地掌握知识。

4. 资源共享：AI平台能够整合优质教育资源，打破地域限制，实现教育资源的公平分配。

二、AI教育的负面影响

1. 教育公平问题：AI技术的应用可能导致教育资源分配不均，加剧教育差距，使教育公平问题更加突出。

2. 教师职业转型：AI技术可能取代部分教师的工作，引发职业危机，要求教师必须提升自身技能，以适应教育变革。

3. 学生过度依赖：学生可能过度依赖AI技术，导致自主学习能力下降，影响其终身学习的能力。

4. 隐私泄露风险：AI系统在收集学生数据时，存在隐私泄露的风险，需要加强数据保护措施。

三、AI教育的发展趋势

1. 深度融合：未来，AI技术将在教育领域得到更广泛的应用，与教育深度融合，推动教育变革。

2. 个性化发展：AI技术将推动教育个性化发展，满足不同学生的学习需求，提高教育质量。

3. 教师角色转变：教师需要提升自身技能，适应AI时代的教育变革，成为学生学习的引导者和促进者。

4. 教育监管加强：政府需加强对AI教育应用的监管，确保教育公平和安全，防止技术滥用。

总之，人工智能对现代教育的影响是双刃剑。我们应积极拥抱AI带来的机遇，同时警惕其潜在的负面影响，通过合理的监管和引导，让AI技术在教育领域发挥最大的积极作用。
编辑最终意见:
标题修改建议：

- 标题可以更加精炼，突出文章的核心观点。例如，可以改为：“人工智能教育：机遇与挑战并存”

正文修改意见：

一、AI教育的正面影响

1. 效率提升：AI技术通过自动批改作业，减轻教师负担，并实现个性化教学，显著提高教学效率。
2. 个性化学习：AI系统依据学生个体差异，推荐定制化学习路径和资源，有效提升学习成效。
3. 智能辅导：AI辅助教学能即时解答疑问，提供个性化辅导，助力学生深入理解知识。
4. 资源共享：AI平台整合优质教育资源，突破地域限制，促进教育公平。

二、AI教育的负面影响

1. 教育公平问题：AI技术的应用可能加剧教育资源分配不均，扩大教育差距。
2. 教师职业转型：AI技术可能部分取代教师工作，引发职业焦虑，要求教师提升技能以适应变革。
3. 学生过度依赖：学生可能过度依赖AI技术，导致自主学习能力减弱，影响终身学习能力。
4. 隐私泄露风险：AI系统在收集学生数据时，存在隐私泄露的风险，需加强数据保护。

三、AI教育的发展趋势

1. 深度融合：未来，AI技术将更广泛地应用于教育领域，与教育深度融合，推动教育变革。
2. 个性化发展：AI技术将推动教育个性化，满足不同学生的学习需求，提升教育质量。
3. 教师角色转变：教师需提升技能，适应AI时代，成为学生学习的引导者和促进者。
4. 教育监管加强：政府需加强对AI教育应用的监管，确保教育公平和安全，防止技术滥用。

总体修改建议：

- 在每个小标题下，可以增加一些过渡句，使段落之间的逻辑更加连贯。
- 在论述AI教育的正面和负面影响时，可以适当增加具体案例或数据，增强说服力。
- 在展望AI教育的发展趋势时，可以适当增加对未来技术发展的预测，使文章更具前瞻性。
- 注意全文的语法和用词准确性，确保文章的专业性和严谨性。
