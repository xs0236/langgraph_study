<!-- 7.16 -->

# multi_agent_Supervisor.py

主图
├── 写作子图（已完成）
├── 翻译子图（中→英→日）
├── 配图子图（AI生成图片）
└── 发布子图（自动发公众号/知乎）

这次我们要搭建一个内容工厂：一篇文章从写作 → 翻译 → 配图 → 多平台发布，全流程自动化。
🏗️ 架构设计
plain
┌─────────────────────────────────────────────────────────────┐
│ 主图 (Main Graph) │
│ Supervisor → 写作子图 → 翻译子图 → 配图子图 → 发布子图 → END │
└─────────────────────────────────────────────────────────────┘
↓ ↓ ↓ ↓
┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────────┐
│ 写作子图 │ │ 翻译子图 │ │ 配图子图 │ │ 发布子图 │
│ Research│ │ 中→英 │ │ 生成 │ │ 公众号文案 │
│ Writer │ │ 中→日 │ │ 提示词 │ │ 知乎文案 │
│ Editor │ │ │ │ │ │ 微博文案 │
└─────────┘ └─────────┘ └─────────┘ └─────────────┘

🎯 核心知识点
表格
概念 代码体现
4 个子图嵌套 build_writing_subgraph() / build_translation_subgraph() / build_image_subgraph() / build_publish_subgraph()
子图作为节点 workflow.add_node("writing_team", build_writing_subgraph())
主图总调度 main_supervisor 按阶段顺序调度
状态穿透 主图 article 字段自动传入翻译子图
Stream 全链路追踪 每个子图节点完成都打印

找到问题了！子图状态字段名和主图状态字段名不匹配。
写作子图返回的是 {"draft": ...}，但主图状态查的是 state.get("article")！
plain
子图输出: draft 主图读取: article
↓ ↓
字段名不匹配 → 主图永远读不到 → 无限循环
🔧 修复方案
把写作子图的 draft 改成 article，让字段名一致：
Python

# ========== 【子图 1】写作子图状态 ==========

class WritingState(TypedDict):
topic: str
research_notes: str
article: str # ✅ 把 draft 改成 article！
edit_feedback: str
previous_feedback: str
next_agent: str

# ========== 【子图 1】节点 ==========

def w_writer(state: WritingState):
print(" ✍️ [写作-撰稿] 撰写文章...")
prompt = f"根据调研笔记撰写文章：\n{state['research_notes']}"
return {"article": model.invoke([HumanMessage(content=prompt)]).content} # ✅ 返回 article

def w_editor(state: WritingState):
print(" 📝 [写作-编辑] 审核文章...")
prompt = f"审核以下文章：\n{state['article'][:500]}" # ✅ 读取 article
return {"edit_feedback": model.invoke([HumanMessage(content=prompt)]).content}

def w_supervisor(state: WritingState):
if not state.get("research_notes"):
return {"next_agent": "researcher"}
elif not state.get("article"): # ✅ 检查 article
return {"next_agent": "writer"}
elif not state.get("edit_feedback"):
return {"next_agent": "editor"}
else:
return {"next_agent": "FINISH"}

🎯 问题根因
表格
问题 原因
无限循环写作子图 子图返回 draft，主图查 article，字段名不匹配
子图输出没合并到主图 LangGraph 按字段名匹配合并状态
✅ 修复要点
所有子图状态中的字段名，必须和主图状态中对应的字段名完全一致：
表格
主图字段 子图字段 用途
article article 文章正文
edit_feedback edit_feedback 编辑反馈
english_version english_version 英文翻译
japanese_version japanese_version 日文翻译
image_prompt image_prompt 配图提示词

✅ 流程验证
plain
👔 Main-Supervisor → 阶段1：写作子图
🕵️ 调研 → ✍️ 撰稿 → 📝 编辑
↓
👔 Main-Supervisor → 阶段2：翻译子图
🇺🇸 英文 → 🇯🇵 日文
↓
👔 Main-Supervisor → 阶段3：配图子图
🎨 生成 AI 绘画提示词
↓
👔 Main-Supervisor → 阶段4：发布子图
📱 公众号 → 💡 知乎 → 🔥 微博
↓
👔 Main-Supervisor → 全部完成！
4 个子图串行执行，每个子图内部也正确执行，最终产出 6 份成果！
🏆 你现在拥有的完整技能树
plain
LangGraph 多 Agent 团队
├── 基础
│ ├── StateGraph 状态图构建
│ ├── 条件边 (Conditional Edges)
│ ├── 节点间状态传递
│ └── Checkpointer 持久化 (MemorySaver → SqliteSaver)
│
├── 进阶
│ ├── LLM 动态调度 (Supervisor with Tools/JSON)
│ ├── 并行执行 (Send)
│ ├── 子图嵌套 (Subgraphs)
│ └── Stream 实时输出
│
└── 实战
└── 内容工厂流水线
├── 写作子图 (调研→撰稿→编辑)
├── 翻译子图 (中→英→日)
├── 配图子图 (AI 绘画提示词)
└── 发布子图 (公众号/知乎/微博)

# 🚀 下一步可以做的

表格
方向 说明
接入真实 API 用 DALL-E/Midjourney API 真正生成图片，用微信公众号/知乎 API 真正发布
Web 界面 用 Gradio/Streamlit 做可视化界面，输入 topic 一键生成全套内容
更多子图 加 SEO 优化子图、视频脚本子图、邮件营销子图...
Human-in-the-loop 每个阶段后暂停，让用户确认后再进入下一阶段
