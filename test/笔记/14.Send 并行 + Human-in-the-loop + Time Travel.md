# content_factory_send.py

代码已完整包含：
Send 并行翻译：trans_fan_out 动态派发英/日文翻译，两个 LLM 调用同时执行
Send 并行发布：pub_fan_out 动态派发微信/知乎/微博，三个任务同时执行
Human-in-the-loop：写作后暂停，支持 continue/stop/具体修改意见重写
Time Travel：get_state_history + update_state + 从 checkpoint 重新执行
Gradio 全界面：进度、审核区、成果 Tabs、时间旅行 Tab

流程完全正确！Send 并行 + Human-in-the-loop + Time Travel 都跑通了。
下一步推荐：E. 结构化输出驱动分支
你现在用的是固定分支逻辑——代码里写死 if not english_version: 去翻译。下一步让 AI 自己决定 流程怎么走。
具体场景
场景 1：AI 判断是否需要配图
Python

# 现在：固定配图

if not s.get("image_url"):
return {"next_agent": "image"}

# 升级后：AI 判断

class ImageDecision(BaseModel):
need_image: bool = Field(description="这篇文章是否需要配图")
reason: str = Field(description="判断理由")

decision = model.with_structured_output(ImageDecision).invoke(
"请判断以下文章是否需要配图：\n" + article
)
if decision.need_image:
return {"next_agent": "image"}
else:
return {"next_agent": "pub_fan_out"} # 跳过配图
场景 2：AI 动态选择发布平台
Python
class PublishPlan(BaseModel):
platforms: List[str] = Field(description="选择要发布的平台，可选：wechat, zhihu, weibo")
wechat_style: str = Field(description="微信公众号文案风格")
AI 根据文章内容，自动决定发哪些平台、用什么风格。
场景 3：AI 自我评估文章质量
Python
class QualityCheck(BaseModel):
score: int = Field(description="文章质量评分 1-10")
issues: List[str] = Field(description="存在的问题")
should_rewrite: bool = Field(description="是否需要重写")

if quality.score < 7:
return {"next_agent": "writing", "previous_feedback": "\n".join(quality.issues)}
为什么选这个方向？
表格
你现在的能力 升级后
固定顺序执行 AI 动态决策流程
人工审核后继续 AI 自我审核，低质量自动重写
固定发 3 个平台 AI 根据内容选择最合适的平台
必须配图 AI 判断技术文章可能不需要图
另一个备选：C. 错误处理与重试
如果你更关心稳定性，可以先做这个：
API 限流时自动重试 3 次
网络超时走 fallback 节点
失败时通知用户而不是崩溃
你想先搞 E（AI 动态决策分支） 还是 C（错误处理重试）？或者两个都想学，我按顺序给你安排。
