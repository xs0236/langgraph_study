content_factory_dynamic.py
核心改动：动态图构建
之前
图是编译时固定死的，不管用户需不需要翻译/配图/发布，节点都在那里跑。
现在
点击 🚀 开始生成 时，根据你勾选的模块实时构建一张新图：
Python
def build_graph(features): # features = {"translate": True, "image": False, "publish": True}
mg = StateGraph(MState)
mg.add_node("m_s", m_s)
mg.add_node("writing", writing_sg) # ... 核心节点 ...

    if features.get("translate"):
        mg.add_node("trans_fan_out", trans_fan_out)
        mg.add_node("translate_one", translate_one)
        mg.add_node("trans_aggregate", trans_aggregate)
        # 只勾选了翻译才加这些节点和边

    if features.get("image"):
        mg.add_node("ai_image_decision", ...)
        mg.add_node("image", image_sg)

    if features.get("publish"):
        mg.add_node("ai_publish_plan", ...)
        mg.add_node("pub_fan_out", ...)
        # ...

    return mg.compile(checkpointer=memory)

界面变化
plain
📝 文章主题: [人工智能对现代教育的影响]
[✅] 🌐 翻译 [✅] 🎨 配图 [✅] 📱 发布
[🚀 开始生成]
只勾选「写作+翻译」，不选配图和发布 → 流程变成：
plain
写作 → AI自检 → 人工审核 → 翻译(并行) → FINISH
配图和发布节点根本不存在于这张图中。
使用示例
表格
你勾选的模块 实际执行流程
仅写作 写作 → AI自检 → 人工审核 → FINISH
写作+翻译 写作 → AI自检 → 人工审核 → 翻译 → FINISH
写作+配图 写作 → AI自检 → 人工审核 → AI配图决策 → [可选]配图 → FINISH
写作+发布 写作 → AI自检 → 人工审核 → AI发布计划 → [Send]发布 → FINISH
全部勾选 完整流程（和之前一样）
