# 第四阶段：产品化与面试准备 — 把项目变成产品

> 🕐 **时间：** 第12-19周（最长的一个阶段）
> 🎯 **目标：** 把内容工厂从"能跑"变成"能用"，同时准备面试
> 📁 **对应代码：** `test/content_factory_app.py` → `content_factory_websearch.py`
> 💼 **找工作标准：** 完成本阶段 + 能完整介绍项目

---

## 第12-13周：Human-in-the-loop 深度实践

### 📖 本章核心

前面我们学了基础的 interrupt 用法。本章要做一个**完整的人工审核系统**，支持：

| 能力 | 说明 |
|------|------|
| 多审核点 | 写作后审核、配图后审核、发布前审核 |
| 智能重写 | 用户输入具体修改意见（不是固定 prompt） |
| 编辑反馈闭环 | 把修改意见注入 Prompt，让 AI 针对性改进 |

### 🧩 多审核点架构

```
写作子图 → [human_review: 文章通过?] → 翻译子图 → [human_review: 图片满意?] → 发布子图
                │                              │
          [continue/rewrite/stop]          [continue/regenerate/stop]
```

```python
from langgraph.types import interrupt, Command

def human_review_article(state):
    """审核文章"""
    decision = interrupt({
        "stage": "article_review",
        "article": state.get("article", ""),
        "edit_feedback": state.get("edit_feedback", ""),
        "message": "请输入操作：continue / rewrite / stop"
    })
    return {"human_decision_article": decision}

def human_review_image(state):
    """审核配图"""
    decision = interrupt({
        "stage": "image_review",
        "image_prompt": state.get("image_prompt", ""),
        "message": "请输入操作：continue / regenerate / stop"
    })
    return {"human_decision_image": decision}
```

```python
# 主调度器需要处理多个审核点
def main_supervisor(state):
    # 文章审核
    if state.get("article") and not state.get("human_decision_article"):
        return {"next_agent": "human_review_article"}
    
    human_article = state.get("human_decision_article", "")
    if human_article == "rewrite":
        return {"next_agent": "writing", "article": "", "human_decision_article": ""}
    if human_article == "stop":
        return {"next_agent": "FINISH"}
    
    # 配图审核（文章通过后）
    if state.get("image_prompt") and not state.get("human_decision_image"):
        return {"next_agent": "human_review_image"}
    
    # ...
```

### 🧩 智能重写（核心升级）

用户不再只能输入"continue/rewrite/stop"，而是**直接输入修改意见**：

```python
def main_supervisor(state):
    decision = state.get("human_decision_article", "")
    
    if decision == "stop":
        return {"next_agent": "FINISH"}
    
    if decision == "continue":
        return {"next_agent": "trans"}
    
    # ★ 不是 continue 也不是 stop → 视为具体修改意见！
    # 比如用户输入："请增加关于在线教育的内容"
    # 或者："语气太正式了，写得轻松一点"
    return {
        "next_agent": "writing",
        "article": "",                    # 清空旧文章
        "edit_feedback": "",
        "previous_feedback": decision,    # ★ 把用户意见传给撰稿节点
    }

# 撰稿节点根据反馈改进
def writer_node(state):
    feedback = state.get("previous_feedback", "")
    if feedback:
        prompt = f"根据调研笔记撰写文章，并参考以下修改意见进行改进：\n{feedback}\n\n调研笔记：\n{state['research_notes']}"
    else:
        prompt = f"根据调研笔记撰写文章：\n{state['research_notes']}"
    
    return {"article": model.invoke([HumanMessage(content=prompt)]).content}
```

### 📁 代码文件

| 文件 | 核心功能 |
|------|---------|
| `test/content_factory_hitl.py` | 多审核点 + 智能重写 + Gradio 界面 |
| `test/content_factory_write.py` | 编辑反馈闭环 |

### 💡 任务

1. 运行 `content_factory_hitl.py`，体验完整的人机协作流程
2. 测试：输入"continue"、"请写得活泼一点"、"stop"三种情况
3. **关键理解**：为什么要把用户输入直接当修改意见传给 AI？

---

## 第14周：Time Travel — 状态回溯

### 🤔 什么是 Time Travel？

**Time Travel = 回到任意历史 checkpoint，修改状态后重新执行**

想象这个场景：
```
你运行了内容工厂，生成了文章→翻译→配图→发布
但你发现翻译的语气不对，想重新翻译

没有 Time Travel：需要从头跑一遍（浪费钱和时间）
有 Time Travel：回到翻译前的 checkpoint，清空翻译结果，继续执行
```

### 🧩 Time Travel 的核心 API

```python
# 1. 获取所有历史快照（从最新到最旧）
history = app.get_state_history(config)
for i, state_snapshot in enumerate(history):
    print(f"[{i}] 步骤: {state_snapshot.next} | 配置: {state_snapshot.config}")

# 2. 回到某个历史快照
target_config = history[3].config  # 选第3个快照
# 用 target_config 替换当前的 config

# 3. 修改状态
app.update_state(target_config, {
    "english_version": "",     # 清空英文翻译
    "japanese_version": "",    # 清空日文翻译
})

# 4. 从修改后的状态继续执行
for chunk in app.stream(None, target_config):
    print(chunk)  # 会重新执行翻译！
```

### 🧩 完整实现

```python
def get_checkpoints(config):
    """获取历史快照列表（倒序：最新在最前）"""
    history = app.get_state_history(config)
    checkpoints = []
    for state in history:
        checkpoints.append({
            "config": state.config,
            "next": state.next,          # 下一个要执行的节点
            "values": state.values,      # 当时的完整状态
        })
    return checkpoints

def time_travel(target_idx, config, state_modifications=None):
    """回到指定 checkpoint，可选修改状态，然后重新执行"""
    history = list(app.get_state_history(config))
    target = history[target_idx]
    
    if state_modifications:
        app.update_state(target.config, state_modifications)
    
    # 从 checkpoint 重新执行
    for chunk in app.stream(None, target.config):
        yield chunk
```

### 🎮 使用场景

| 场景 | 操作 |
|------|------|
| 翻译不满意 | 回到翻译前，清空 `english_version` 和 `japanese_version` |
| 配图不好看 | 回到配图前，清空 `image_url` 和 `image_prompt` |
| 发布文案想改 | 回到发布前，清空发布链接 |
| 文章大改 | 回到写作前，清空所有后续产出 |

### 📁 代码文件

| 文件 | 核心功能 |
|------|---------|
| `test/content_factory_timetravel.py` | Time Travel + Gradio 界面 + 快捷按钮 |

### 💡 任务

1. 运行 `content_factory_timetravel.py`
2. 完整跑一次流程
3. 进入"时间旅行"Tab，刷新历史，选择一个 checkpoint 回到过去
4. **理解**：为什么 Time Travel + 状态修改 + 重新执行能组合出这么强大的功能？

---

## 第15周：AI 动态决策分支

### 🤔 为什么让 AI 自己决策？

目前的流程是**固定**的：写作 → 翻译 → 配图 → 发布。

但有些文章不需要配图（如短评），有些不需要翻译（如中文本地文章）。让 AI 自己判断：

```python
# 固定的流程（现在）
文章 → 配图 → 翻译 → 发布

# AI 动态决策（本周目标）
文章 → [AI 判断: 需要配图?] → 是→配图, 否→跳过
     → [AI 判断: 需要翻译?] → 是→翻译, 否→跳过
     → [AI 判断: 发哪些平台?] → 微信? 知乎? 微博?
```

### 🧩 结构化输出

让 LLM 输出**结构化的 JSON**，而不是自由文本：

```python
from pydantic import BaseModel, Field
from typing import List

# 定义 AI 决策的输出格式
class QualityCheck(BaseModel):
    """AI 自检结果"""
    score: int = Field(description="文章质量评分 1-10")
    issues: List[str] = Field(description="存在的问题列表")
    should_rewrite: bool = Field(description="是否需要重写")

class ImageDecision(BaseModel):
    """配图决策"""
    need_image: bool = Field(description="这篇文章是否需要配图")
    reason: str = Field(description="判断理由")

class PublishPlan(BaseModel):
    """发布计划"""
    platforms: List[str] = Field(description="选择要发布的平台")
    wechat_style: str = Field(description="公众号风格建议")
    zhihu_style: str = Field(description="知乎风格建议")

# 绑定结构化输出
quality_model = model.with_structured_output(QualityCheck)
image_model = model.with_structured_output(ImageDecision)
publish_model = model.with_structured_output(PublishPlan)
```

### 🧩 AI 决策节点

```python
def ai_quality_check(state):
    """AI 自检：给文章打分"""
    result = quality_model.invoke([
        HumanMessage(content=f"请评估以下文章质量：\n{state['article']}")
    ])
    
    print(f"📊 AI 评分：{result.score}/10")
    if result.issues:
        print(f"💡 问题：{', '.join(result.issues)}")
    
    return {
        "ai_quality_score": result.score,
        "ai_quality_issues": "\n".join(result.issues),
    }

def ai_image_decision(state):
    """AI 判断是否需要配图"""
    result = image_model.invoke([
        HumanMessage(content=f"以下文章是否需要配图？\n{state['article'][:500]}")
    ])
    
    print(f"🎨 AI 配图决策：{'需要' if result.need_image else '不需要'}（{result.reason}）")
    return {
        "ai_need_image": result.need_image,
        "ai_image_reason": result.reason,
    }

def ai_publish_plan(state):
    """AI 制定发布计划"""
    result = publish_model.invoke([
        HumanMessage(content=f"为以下文章选择最佳发布平台：\n{state['article'][:500]}")
    ])
    
    return {
        "ai_publish_platforms": ",".join(result.platforms),
        "ai_publish_styles": f"微信：{result.wechat_style}；知乎：{result.zhihu_style}",
    }
```

### 🚀 新流程

```
写作子图 → AI自检(quality < 7? → 自动重写)
         → 人工审核(continue/意见/stop)
         → 翻译(Send并行)
         → AI配图决策(need_image? → [是]配图 [否]跳过)
         → AI发布计划(选平台)
         → 发布(Send并行，只发选中平台)
```

### 📁 代码文件

| 文件 | 核心功能 |
|------|---------|
| `test/content_factory_ai_decision.py` | AI 动态决策完整实现 |

### 💡 任务

1. 运行 `content_factory_ai_decision.py`
2. 观察 AI 如何判断是否需要配图
3. 观察 AI 如何选择发布平台
4. **理解**：结构化输出 vs 自由文本输出的优缺点

---

## 第16周：动态图构建

### 🤔 什么是动态图？

**动态图 = 运行时根据用户选择，实时构建不同结构的图**

```
用户勾选了"翻译"+"发布"：
  主图: Supervisor → 写作 → 翻译 → 发布 → END

用户只勾选"写作"：
  主图: Supervisor → 写作 → END
  
用户勾选全部：
  主图: Supervisor → 写作 → 翻译 → 配图 → 发布 → END
```

### 🧩 动态构图实现

```python
def build_graph(features):
    """根据功能配置动态构建图"""
    # features = {"translate": True, "image": True, "publish": True}
    
    mg = StateGraph(MainState)
    
    # === 核心节点（始终存在）===
    mg.add_node("supervisor", main_supervisor)
    mg.add_node("writing", writing_subgraph)
    mg.add_node("ai_quality_check", ai_quality_check)
    mg.add_node("human_review", human_review)
    
    # === 可选节点（根据用户配置）===
    if features.get("translate"):
        mg.add_node("trans", translation_subgraph)
    
    if features.get("image"):
        mg.add_node("ai_image_decision", ai_image_decision)
        mg.add_node("image", image_subgraph)
    
    if features.get("publish"):
        mg.add_node("ai_publish_plan", ai_publish_plan)
        mg.add_node("publish", publish_subgraph)
    
    # === 动态配置条件边 ===
    mg.set_entry_point("supervisor")
    
    # 构建条件边映射
    cond_map = {
        "writing": "writing",
        "ai_quality_check": "ai_quality_check",
        "human_review": "human_review",
        "FINISH": END,
    }
    if features.get("translate"):
        cond_map["trans"] = "trans"
    if features.get("image"):
        cond_map["ai_image_decision"] = "ai_image_decision"
        cond_map["image"] = "image"
    if features.get("publish"):
        cond_map["ai_publish_plan"] = "ai_publish_plan"
        cond_map["publish"] = "publish"
    
    mg.add_conditional_edges("supervisor", lambda s: s["next_agent"], cond_map)
    
    # 动态加边
    for node in ["writing", "ai_quality_check", "human_review"]:
        mg.add_edge(node, "supervisor")
    if features.get("image"):
        mg.add_edge("ai_image_decision", "supervisor")
        mg.add_edge("image", "supervisor")
    if features.get("translate"):
        mg.add_edge("trans", "supervisor")
    if features.get("publish"):
        mg.add_edge("publish", "supervisor")
    
    return mg.compile(checkpointer=memory)
```

### 🖼️ 前端联动

```python
# Gradio 界面
with gr.Row():
    translate_checkbox = gr.Checkbox(label="🌐 翻译", value=True)
    image_checkbox = gr.Checkbox(label="🎨 配图", value=True)
    publish_checkbox = gr.Checkbox(label="📱 发布", value=True)

# 根据勾选构建不同的图
features = {
    "translate": translate_checkbox,
    "image": image_checkbox,
    "publish": publish_checkbox,
}
graph = build_graph(features)
```

### 📁 代码文件

| 文件 | 核心功能 |
|------|---------|
| `test/content_factory_dynamic.py` | 动态图构建完整实现 |

### 💡 任务

1. 运行 `content_factory_dynamic.py`
2. 分别测试勾选不同组合的执行流程
3. **理解**：为什么动态构图对产品化很重要？

---

## 第17周：LangSmith 全链路监控

### 🤔 为什么需要监控？

```
没有监控：
  用户说"文章生成太慢了"
  你：不知道哪个节点慢，不知道用了多少 Token，不知道在哪报错

有监控（LangSmith）：
  打开 LangSmith 网页：
  → 写作子图 5.2s，用了 1500 Token
  → 翻译子图 3.8s，用了 800 Token
  → 配图子图 1.2s，用了 200 Token
  → 原来写作最慢！优化它的 prompt 减少 Token 消耗
```

### 🧩 接入 LangSmith（零代码侵入！）

```python
import os

# 只需要设置 3 个环境变量
os.environ["LANGSMITH_TRACING"] = "true"      # 启用追踪
os.environ["LANGSMITH_API_KEY"] = "ls-你的key" # API Key
os.environ["LANGSMITH_PROJECT"] = "ai-content-factory"  # 项目名

# 编译和运行完全不需要改代码！
app = mg.compile(checkpointer=memory)
app.stream(inputs, config)  # ← 自动追踪！
```

### 📊 LangSmith 能看什么

| 信息 | 用处 |
|------|------|
| Trace 列表 | 每次运行生成一条 Trace |
| 节点调用链 | 展开看到 m_s → writing → trans 的完整路径 |
| 输入/输出 | 每个节点的输入状态和输出状态 |
| Token 消耗 | 每个 LLM 调用用了多少 Token |
| 延迟 | 每个节点执行耗时 |
| 状态变化 | LangGraph 特有的 checkpoint 状态 diff |

### 🏭 生产环境进阶

```python
# 给 Trace 打标签（区分用户/场景）
config = {
    "configurable": {"thread_id": "user_123"},
    "metadata": {
        "user_id": "user_123",
        "feature_set": "translate+image",
        "topic": "AI教育"
    }
}

# 采样追踪（节省额度）
import random
os.environ["LANGSMITH_TRACING"] = "true" if random.random() < 0.1 else "false"
# 只追踪 10% 的请求
```

### 📁 代码文件

| 文件 | 核心功能 |
|------|---------|
| `test/content_factory_langsmith.py` | LangSmith 完整接入 |

### 💡 任务

1. 注册 LangSmith 账号（https://smith.langchain.com）
2. 配置 API Key
3. 运行 `content_factory_langsmith.py`
4. 在 LangSmith 网页上查看 Trace

---

## 第18周：Web Search & RAG

### 🤔 为什么需要搜索？

```
没有搜索：
  AI 写"2026年AI最新政策" → 只能凭训练数据编造
  → 可能引用过时信息，甚至完全编造

有搜索：
  AI 先去 DuckDuckGo 搜索"2026年AI政策"
  → 拿到真实新闻和官方文件
  → 基于真实数据写文章
  → 质量飞跃！
```

### 🧩 DuckDuckGo 搜索（免费，无需 API Key）

```python
from duckduckgo_search import DDGS

def web_search(query: str, max_results: int = 5) -> str:
    """使用 DuckDuckGo 搜索"""
    try:
        with DDGS() as ddgs:
            results = list(ddgs.text(query, max_results=max_results))
        
        if not results:
            return ""
        
        formatted = []
        for r in results:
            formatted.append(f"- {r.get('title', '')}\n  {r.get('body', '')}\n  来源: {r.get('href', '')}")
        
        return "\n\n".join(formatted)
    except Exception as e:
        print(f"⚠️ 搜索失败: {e}")
        return ""  # 搜索失败不崩溃，回退到模型知识
```

### 🧩 改造调研节点

```python
def researcher_node(state):
    """调研节点：先搜索，再基于真实数据写笔记"""
    topic = state["topic"]
    
    # 搜索互联网
    print(f"🔍 正在搜索: {topic}")
    search_results = web_search(topic, max_results=5)
    
    if search_results:
        # 有搜索结果 → 基于真实数据写
        prompt = f"""针对主题 '{topic}' 搜集关键信息。
        
以下是从互联网搜索到的真实参考资料：
{search_results}

请基于以上真实数据，输出简短但有信息量的调研笔记。"""
    else:
        # 搜索失败 → 回退到模型知识
        prompt = f"针对主题 '{topic}' 搜集关键信息，输出简短调研笔记。"
    
    return {"research_notes": model.invoke([HumanMessage(content=prompt)]).content}
```

### 🧩 RAG（检索增强生成）

RAG = 在生成回答前，先从自己的知识库中检索相关内容。

```
用户提问
    │
    ▼
┌──────────────┐    ┌──────────────────┐
│ 检索知识库   │───→│ 找到相关文档片段  │
└──────────────┘    └──────────────────┘
    │
    ▼
┌──────────────┐    ┌──────────────────┐
│ LLM 生成回答  │───→│ 基于检索到的内容  │ ← 有依据，不瞎编！
└──────────────┘    └──────────────────┘
```

使用 RAGFlow 作为知识库：

```python
def ragflow_retrieve(query, dataset_id, top_k=5):
    """从 RAGFlow 知识库检索相关片段"""
    url = "http://localhost:9380/api/retrieval"
    headers = {"Authorization": "Bearer your-api-key"}
    payload = {
        "dataset_id": dataset_id,
        "question": query,
        "top_k": top_k,
        "rerank": True,  # 启用重排序
    }
    resp = requests.post(url, json=payload, headers=headers)
    chunks = resp.json()["data"]["chunks"]
    return format_chunks(chunks)

# 混合检索：知识库 + 网络
def hybrid_research(topic):
    rag_results = ragflow_retrieve(topic, dataset_id="your_kb_id")
    web_results = web_search(topic)
    return f"【知识库】{rag_results}\n\n【网络】{web_results}"
```

### 📁 代码文件

| 文件 | 核心功能 |
|------|---------|
| `test/content_factory_websearch.py` | Web Search 接入 |
| `test/笔记/19.RAG 增强.md` | RAGFlow 方案设计 |

### 💡 任务

1. 安装 `duckduckgo-search`：`pip install duckduckgo-search`
2. 运行 `content_factory_websearch.py`
3. 对比：有搜索的文章 vs 没有搜索的文章，质量差多少
4. （选做）了解 RAGFlow 的工作原理

---

## 第19周：面试准备

### 📋 面试知识图谱

```
LangGraph 面试知识体系
├── 概念题（40%）
│   ├── StateGraph 是什么？
│   ├── Node 和 Edge 的关系
│   ├── Checkpointer 的作用
│   ├── interrupt 机制的原理
│   ├── Send 和并行的关系
│   ├── Subgraph 解决了什么问题
│   └── Stream 和 invoke 的区别
│
├── 对比题（20%）
│   ├── MemorySaver vs SqliteSaver
│   ├── invoke vs stream
│   ├── Node vs Subgraph
│   ├── interrupt_before vs interrupt()
│   └── 普通边 vs 条件边
│
├── 场景题（20%）
│   ├── 设计一个客服 Agent 系统
│   ├── 设计一个内容生成平台
│   ├── 如何做人工审批流程？
│   └── 如何优化 Agent 响应速度？
│
└── 项目题（20%）
    ├── 介绍你的内容工厂项目
    ├── 你在项目中遇到的难点
    ├── 你是怎么解决这些难点的
    └── 如果再让你重做一次，你会怎么改？
```

### 🎯 概念题标准答案

**Q：StateGraph 和普通 Graph 有什么区别？**

A：StateGraph 是"有状态的图"。普通 Graph 只关心节点间的连接关系，而 StateGraph 在每个节点执行时都能读写一个共享的 State 对象。节点 A 写入的数据，节点 B 可以直接读取。State 由 Checkpointer 自动持久化，支持中断恢复和时间旅行。

**Q：Checkpointer 是怎么工作的？**

A：每执行完一个节点，Checkpointer 自动保存当前 State 的完整快照到存储后端（内存/SQLite/PostgreSQL）。保存时绑定 thread_id 区分不同会话。恢复时，将 State 恢复到指定 checkpoint，然后从那里继续执行。这让 Agent 拥有了"记忆"和"时光倒流"的能力。

**Q：interrupt() 和 interrupt_before 有什么区别？**

A：`interrupt_before` 是编译时指定的断点（如 `compile(interrupt_before=["tools"])`），在执行到指定节点前自动暂停。`interrupt()` 是在节点函数内部显式调用的暂停点，更灵活，可以携带自定义 payload 给外部代码读取。`interrupt_before` 适合简单的全局断点，`interrupt()` 适合需要人类交互的复杂场景。

**Q：Send 的作用是什么？有什么限制？**

A：Send 用于并行派发任务。把同一个节点用不同参数同时执行 N 次。限制：Send 只能在条件边（`add_conditional_edges`）的 mapper 函数中返回，不能在普通节点中返回。因为普通节点必须返回 `dict` 来更新状态，而 Send 返回的是 `list[Send]`。

**Q：Subgraph 解决了什么问题？**

A：模块化和复用。当图变得很大（20+ 节点），把它拆成多个子图，主图只关心高层次调度。子图内部自治，独立开发和测试。子图可以被不同主图复用。关键点：子图和主图的字段名必须一致，否则状态传不过去。

### 🏗️ 系统设计题：设计客服 Agent 系统

```python
# 架构设计
#
# ┌────────────────────────────────────────────┐
# │              客服 Agent 系统                 │
# │                                             │
# │  用户输入 → 意图识别(LLM) → [查订单]→[查物流] │
# │                            → [退换货]→[人工] │
# │                            → [咨询]→[FAQ]   │
# │                                             │
# │  关键设计点：                                  │
# │  1. 多 Agent 分诊（路由到专业 Agent）            │
# │  2. 人工干预（复杂问题转人工）                    │
# │  3. 持久化记忆（跨会话的用户上下文）               │
# │  4. LangSmith 监控（分析瓶颈）                  │
# └────────────────────────────────────────────┘

# 你的回答思路：
# 1. Supervisor 做意图识别和路由
# 2. 每个意图对应一个专业 Agent（订单/物流/售后）
# 3. 置信度低于阈值时，interrupt 转人工
# 4. SqliteSaver 持久化对话历史
# 5. LangSmith 追踪每个 Agent 的 Token 消耗和延迟
```

### 📝 面试常考的奇怪问题（视频面经汇总）

面试中经常出现一些"奇怪"的问题，看似不按套路出牌，但考察的都是**核心原理和工程思维**：

| 奇怪问题 | 考点 | 
|---------|------|
| "AI 回复错了，你怪 AI 还是怪代码？" | 系统排查思维 |
| "让 Agent 自己写自己的代码，能行吗？" | AI 边界认知 |
| "用户拿 AI 文章当论文发表了，谁负责？" | 产品责任意识 |
| "老板说 AI 写得太差要换人工，你怎么回？" | 沟通 + 替代方案 |
| "用户让 Agent 骂人怎么防？" | 安全防护多层设计 |
| "100 人同时用，SQLite 能扛住吗？" | 架构扩展性 |
| "LLM 拒绝回答，Agent 怎么办？" | 容错设计 |
| "每次调用都要钱，怎么省钱？" | 成本控制 |
| "服务挂了能恢复吗？" | 持久化理解 |

> 📖 **完整 30 道面试题+深度解析** 见：`面试常见问题与深度解析.md`

### 📝 项目介绍模板

```
项目名称：AI 内容工厂
项目描述：一个基于 LangGraph 的多 Agent 内容生成平台
技术栈：LangGraph + ChatOpenAI(智谱 GLM) + Gradio + SQLite + DuckDuckGo

核心功能：
1. 多 Agent 团队：Supervisor 调度 Researcher/Writer/Editor
2. 4 个子图嵌套：写作/翻译/配图/发布
3. 人工审核：interrupt + Command(resume=) 实现
4. 并行执行：Send 机制并行翻译和发布
5. 动态构图：运行时根据用户配置动态构建图
6. Time Travel：回到任意 checkpoint 重新执行
7. AI 自检：Pydantic 结构化输出实现质量评分
8. Web Search：DuckDuckGo 搜索增强调研质量

我的角色：独立完成全部设计和编码

技术难点：
1. LangGraph 状态覆盖机制（update_state + as_node）
2. Send 在动态图中的兼容性（条件边 vs 普通节点）
3. 多审核点下的人类决策路由
```

### 💡 第19周任务

1. 对照面试题清单，每个题自问自答
2. 整理内容工厂项目文档（画出架构图）
3. 准备一个 5 分钟的项目演示
4. 找一个朋友做模拟面试

---

## 🏆 第四阶段验收清单

### 产品化能力

| 能力 | 文件 | 掌握了吗？ |
|------|------|-----------|
| Web 界面 | `content_factory_app.py` | □ |
| 多审核点 | `content_factory_hitl.py` | □ |
| 智能重写 | `content_factory_write.py` | □ |
| Time Travel | `content_factory_timetravel.py` | □ |
| AI 决策 | `content_factory_ai_decision.py` | □ |
| 动态构图 | `content_factory_dynamic.py` | □ |
| 全链路监控 | `content_factory_langsmith.py` | □ |
| Web Search | `content_factory_websearch.py` | □ |
| RAG 了解 | `笔记/19.RAG 增强.md` | □ |

### 面试能力

| 类型 | 准备了吗？ |
|------|-----------|
| 概念题（10 道） | □ |
| 对比题（5 道） | □ |
| 场景题（4 道） | □ |
| 项目介绍 | □ |
| 系统设计 | □ |

### 找工作 Checklist

- [ ] 完成所有 content_factory 系列代码的运行和理解
- [ ] 能独立写出一个带人工审批的多 Agent 团队
- [ ] 能解释 StateGraph、Checkpointer、interrupt、Send、Subgraph 的原理
- [ ] 有一份完整的项目介绍（含架构图）
- [ ] 能展示内容工厂的完整 Demo
- [ ] 做过至少一次模拟面试

---

## 🚀 第四阶段总结

### 你学到的核心能力

```
✅ 多审核点 Human-in-the-loop
✅ 智能重写（用户意见注入）
✅ Time Travel 状态回溯
✅ AI 动态决策（结构化输出）
✅ 动态图构建
✅ LangSmith 全链路监控
✅ Web Search & RAG
✅ 面试准备
```

### 你现在的能力水平

你已经达到了**找工作水平**：
- 理解 LangGraph 全部核心概念
- 拥有一个完整的内容工厂项目
- 踩过大部分常见的坑
- 能设计中等复杂度的 Agent 系统

### 下一步

找工作不是终点。如果你真的想成为**专业的 AI 专家**，请继续：

> 📖 打开 `第五阶段-AI大神进阶.md` 开始真正的深度之旅
