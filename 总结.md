# LangGraph 知识体系总纲

> 🎯 **目标读者：** AI/编程零基础 → 中期找工作 → 长期AI大神
> 📍 **学习方式：** 代码能跑通（test/目录已有），现在需要系统理解概念与原理

---

## 一、什么是 LangGraph？

LangGraph 是一个 **有状态的多 Agent 编排框架**。它的本质是：

> **把 AI 工作流画成一张"图"，图的每个节点（Node）就是一步操作，边（Edge）就是操作之间的流转逻辑。**

```
       ┌──────────┐
       │ 用户输入  │
       └────┬─────┘
            ▼
   ┌────────────────┐
   │  Node: 理解意图 │  ← 节点1：LLM 处理
   └───────┬────────┘
           │ 条件判断（Conditional Edge）
           ▼
   ┌────────────────┐     ┌────────────────┐
   │ Node: 查天气   │     │ Node: 查计算器  │
   └───────┬────────┘     └───────┬────────┘
           │                      │
           └──────────┬───────────┘
                      ▼
             ┌────────────────┐
             │  Node: 汇总回答 │
             └───────┬────────┘
                     ▼
              ┌─────────────┐
              │  输出给用户   │
              └─────────────┘
```

### 核心定位

| 对比 | 传统编程 | LangGraph |
|------|---------|-----------|
| 流程控制 | if-else / switch-case | Conditional Edges |
| 状态管理 | 数据库 / 全局变量 | Checkpointer（自动持久化） |
| 并行 | 多线程 / asyncio | Send 机制（原生并行） |
| 人工干预 | 需自己实现 | interrupt() 原生支持 |
| 可观测性 | 日志 | LangSmith 全链路追踪 |

---

## 二、Core Concepts（核心概念速查）

### 2.1 StateGraph — 状态图

LangGraph 的核心类。你把节点和边注册到图上，编译后就得到一个可执行的 `app`。

```python
from langgraph.graph import StateGraph

# 1. 定义状态结构
class MyState(TypedDict):
    messages: list
    result: str

# 2. 建图
builder = StateGraph(MyState)

# 3. 加节点
builder.add_node("node_a", my_function)

# 4. 加边
builder.add_edge("node_a", "node_b")
builder.add_conditional_edges("node_a", routing_function, {...})

# 5. 编译
app = builder.compile()
```

### 2.2 State（状态）— 共享白板

**状态是整个图的"共享白板"**。每个节点都能读写，下一个节点能看到上一个节点写的内容。

```python
class TeamState(TypedDict):
    messages: Annotated[list, add_messages]  # 自动追加模式
    topic: str                                # 主题
    research_notes: str                       # 调研笔记（研究员写入）
    draft: str                                # 初稿（撰稿人写入）
    edit_feedback: str                        # 编辑意见（编辑写入）
    next_agent: str                           # 下一节点（主管写入）
```

> **💡 关键理解：** State 不是数据库，它是"当前工作记忆"。Checkpointer 负责把它持久化到磁盘。

### 2.3 Node（节点）— 工作单元

Node 就是一个 Python 函数：**输入 State → 干活 → 输出 State 更新**

```python
def researcher_node(state: TeamState):
    """调研员：只负责搜集资料"""
    topic = state["topic"]
    # 调用 LLM
    response = model.invoke([HumanMessage(content=f"调研 {topic}")])
    return {"research_notes": response.content}  # ← 写回 State
```

**黄金法则：**
- 每个 Node 只干一件事
- 只从 State 读你需要的数据
- 只写你负责的字段

### 2.4 Edge（边）— 流转路径

| 类型 | 用途 | 代码 |
|------|------|------|
| **普通边** | 固定路径 A → B | `add_edge("a", "b")` |
| **条件边** | 根据 State 动态决定去向 | `add_conditional_edges("a", router, mapping)` |
| **入口点** | 流程从哪里开始 | `set_entry_point("node_a")` |

### 2.5 Checkpointer（检查点）— Agent 的记忆

让 Agent 记住"上一次聊到哪了"。

```python
from langgraph.checkpoint.memory import MemorySaver
from langgraph.checkpoint.sqlite import SqliteSaver

# 内存版（重启丢）
memory = MemorySaver()
# 磁盘版（重启不丢）
memory = SqliteSaver.from_conn_string("sqlite:///checkpoints.db")

app = workflow.compile(checkpointer=memory)
config = {"configurable": {"thread_id": "会话ID"}}
```

**核心机制：** 每执行完一个节点，Checkpointer 自动保存完整 State 快照。

### 2.6 interrupt（中断）— 人工踩刹车

让流程在某个节点暂停，等人类确认后再继续。

```python
from langgraph.types import interrupt, Command

def review_node(state):
    """人类审核节点"""
    # 暂停！把文章抛给人类
    decision = interrupt({
        "article": state["article"],
        "message": "请审核这篇文章"
    })
    return {"approved": decision == "continue"}
```

**恢复执行：**
```python
# 从断点继续，传入人类决策
app.stream(Command(resume="continue"), config)
```

### 2.7 Send（发送）— 并行派发任务

把一个任务拆成多个，同时执行。

```python
from langgraph.types import Send

def fan_out(state):
    return [
        Send("worker", {"angle": "技术角度", "topic": state["topic"]}),
        Send("worker", {"angle": "教育角度", "topic": state["topic"]}),
        Send("worker", {"angle": "伦理角度", "topic": state["topic"]}),
    ]
```

### 2.8 Subgraph（子图）— 图中的图

把一组节点封装成一个子图，主图把它当一个节点用。

```python
# 定义子图
writing_subgraph = StateGraph(MyState)
writing_subgraph.add_node(...)  # 内部节点
# ... 内部边 ...
writing_sg = writing_subgraph.compile()

# 主图使用子图
main_graph.add_node("writing_team", writing_sg)
```

---

## 三、学习路线全景图

```
第一阶段：基础入门（4周）
├── 第0周：Python与AI基础
├── 第1周：第一个Agent — ReAct模式
├── 第2周：状态管理与记忆
├── 第3周：条件路由与分支
└── 📁 test/my_first_agent.py

第二阶段：多Agent协作（4周）
├── 第4周：Supervisor调度模式
├── 第5周：人工干预 Human-in-the-loop
├── 第6周：LLM动态调度
├── 第7周：持久化存储
└── 📁 test/multi_agent_*.py

第三阶段：企业级实战（4周）
├── 第8周：并行执行 Send
├── 第9周：子图嵌套 Subgraph
├── 第10周：Stream实时输出
├── 第11周：内容工厂V1
└── 📁 test/multi_agent_Send.py ～ multi_agent_Supervisor.py

第四阶段：产品化与面试（7周）
├── 第12-13周：Human-in-the-loop深度实践
├── 第14周：Time Travel 状态回溯
├── 第15周：AI动态决策
├── 第16周：动态图构建
├── 第17周：LangSmith监控
├── 第18周：Web Search & RAG
├── 第19周：面试准备
└── 📁 test/content_factory_*.py

第五阶段：AI大神进阶（持续）
├── 源码深度研究
├── 高级架构模式
├── AI工程化全栈
├── 系统设计
└── 前沿方向探索
```

---

## 四、test/ 目录代码文件索引

### 基础篇
| 文件 | 核心学什么 | 难度 |
|------|-----------|------|
| `my_first_agent.py` | ReAct Agent、StateGraph、Tool绑定、ChatOpenAI | ⭐ |
| `multi_agent_team.py` | Supervisor模式、TeamState共享状态、条件路由 | ⭐⭐ |

### 多Agent协作篇
| 文件 | 核心学什么 | 难度 |
|------|-----------|------|
| `multi_agent_with_memory.py` | Checkpointer团队记忆 | ⭐⭐ |
| `multi_agent_with_human.py` | interrupt_before + 人工审批 | ⭐⭐⭐ |
| `multi_agent_with_human_new.py` | 重写循环、State覆盖 | ⭐⭐⭐ |
| `multi_agent_official.py` | interrupt()官方机制 | ⭐⭐⭐ |
| `multi_agent_humantool.py` | LLM动态调度Supervisor | ⭐⭐⭐ |

### 进阶篇
| 文件 | 核心学什么 | 难度 |
|------|-----------|------|
| `multi_agent_sqlitesaver.py` | SqliteSaver持久化 | ⭐⭐ |
| `multi_agent_Send.py` | Send并行执行 | ⭐⭐⭐ |
| `multi_agent_Subgraphs.py` | 子图嵌套 | ⭐⭐⭐ |
| `multi_agent_Stream.py` | Stream实时输出 | ⭐⭐⭐ |
| `multi_agent_Supervisor.py` | 4个子图嵌套内容工厂 | ⭐⭐⭐⭐ |
| `multi_agent_api.py` | 真实API接入 | ⭐⭐⭐⭐ |

### 企业级实战篇（内容工厂系列）
| 文件 | 核心学什么 | 难度 |
|------|-----------|------|
| `content_factory_app.py` | Gradio Web界面 | ⭐⭐⭐ |
| `content_factory_hitl.py` | 多审核点 + 智能重写 | ⭐⭐⭐⭐ |
| `content_factory_write.py` | 编辑反馈闭环 | ⭐⭐⭐⭐ |
| `content_factory_timetravel.py` | Time Travel状态回溯 | ⭐⭐⭐⭐⭐ |
| `content_factory_send.py` | Send + HitL + TimeTravel三合一 | ⭐⭐⭐⭐⭐ |
| `content_factory_ai_decision.py` | AI动态决策分支 | ⭐⭐⭐⭐⭐ |
| `content_factory_dynamic.py` | 动态图构建 | ⭐⭐⭐⭐⭐ |
| `content_factory_langsmith.py` | LangSmith全链路监控 | ⭐⭐⭐ |
| `content_factory_websearch.py` | Web Search集成 | ⭐⭐⭐⭐ |

---

## 五、核心原理总结

### 5.1 状态驱动（State-Driven）

**LangGraph 的最底层原理：一切围绕 State 转。**

```
节点A 写入 State → Checkpointer 保存 → 条件边读取 State → 决定去节点B还是C
                                                          ↓
                                                  节点B 读取 State → 写入新内容 → ...
```

> **理解了这个，你就理解了 LangGraph 的一半。**

### 5.2 图执行引擎（Pregel 架构）

LangGraph 受 Google Pregel 论文启发，采用 **"超级步（Superstep）"** 执行模型：

1. 所有可执行的节点并行启动
2. 每个节点读取输入、执行、写入输出
3. 等所有节点完成后，进入下一步
4. 重复直到没有节点需要执行

### 5.3 Checkpointer 机制

```
执行前                    执行后
┌──────┐   节点A     ┌──────┐
│State │───────────→│State'│──→ 自动保存到 Checkpointer
│ v1.0 │  运行并修改  │ v2.0 │
└──────┘            └──────┘
                          │
                    时间旅行：可以回退到任意历史版本
```

### 5.4 interrupt 完整生命周期

```
1. 节点调用 interrupt() → 图暂停
2. Checkpointer 保存当前 State
3. 外部代码读取 State（获取文章等）
4. 人类做出决策
5. 外部代码调用 Command(resume=决策)
6. 图从断点恢复，interrupt() 返回人类决策
7. 节点继续执行
```

### 5.5 常见陷阱（踩坑记录）

| 陷阱 | 现象 | 解决方案 |
|------|------|---------|
| **Supervisor无限循环** | 主管一直在派活 | 加 iteration_count 硬性退出条件 |
| **状态覆盖失败** | update_state 后旧数据还在 | 用 as_node 参数或追加消息 |
| **重放缓存** | 打回重写输出完全一样 | temperature≠0 + 把编辑反馈注入 Prompt |
| **增量 vs 快照** | invoke 返回值没有预期字段 | 用 get_state(config).values 拿完整快照 |
| **Send 返回类型错误** | "Expected dict, got list[Send]" | Send 只能用于条件边，不能用于普通节点 |
| **字段名不匹配** | 子图返回 draft，主图查 article | 子图和主图的字段名必须一致 |

---

## 六、学习方法论

### 阶段 | 方法 | 占比
---|---|---
打基础 | 读笔记 → 跑代码 → 改代码实验 | 70%实操 + 30%理论
进阶 | 读源码 → 画架构图 → 写总结 | 50%阅读 + 50%动手
高手 | 读论文 → 做项目 → 教别人 | 30%输入 + 70%输出

### 核心建议
1. **每一行代码都要理解为什么** — 不要跑通就过
2. **每学一个新概念，画出它的 State 流转图** — 可视化是理解 LangGraph 的最佳方式
3. **把错误信息当老师** — LangGraph 的错误信息非常精准，读懂它=精通它
4. **对比学习** — 对比 MemorySaver vs SqliteSaver、invoke vs stream、interrupt_before vs interrupt()
5. **项目驱动** — 每完成一个阶段，用考核项目检验自己

---

> ⚠️ **重要提示：** test/ 目录只覆盖了 LangGraph 约 **60%** 的功能。完整功能全景见 `功能全景与补全学习路线.md`

> 📖 **下一步：** 打开 `PRD-学习路线图.md` 查看完整学习计划
