# LangGraph 功能全景图与补全学习路线

> 🎯 **目的：** test/ 目录只覆盖了 LangGraph 的一部分功能。本文档系统梳理 LangGraph **全部功能模块**，标记哪些你已学、哪些缺失，并规划补全路线。
> 📍 **信息来源：** 源码分析 (`libs/langgraph/langgraph/`) + 官方文档 + SDK + CLI

---

## 一、功能全景图

```
LangGraph 功能全景
│
├── 一、核心图构建 ★★★★★ (test/已覆盖基础)
│   ├── StateGraph ★★★★ (my_first_agent, multi_agent_team)
│   ├── Node/Edge ★★★★
│   ├── Conditional Edge ★★★★
│   ├── Subgraph ★★★ (multi_agent_Subgraphs)
│   ├── Send 并行 ★★★ (multi_agent_Send)
│   ├── ⚠️ add_sequence() 序列边 — 未覆盖
│   ├── ⚠️ input_schema/output_schema — 未覆盖
│   ├── ⚠️ context_schema — 未覆盖
│   ├── ⚠️ set_conditional_entry_point — 未覆盖
│   └── ⚠️ set_finish_point — 未覆盖
│
├── 二、状态管理 ★★★★★
│   ├── TypedDict State ★★★★
│   ├── Reducer (add_messages等) ★★★
│   ├── Checkpointer (Memory/SQLite/Postgres) ★★★★
│   ├── ⚠️ 自定义 Reducer — 未覆盖
│   ├── ⚠️ Pydantic/DataClass State — 未覆盖
│   ├── ⚠️ Channels 底层 (Topic/LastValue等) — 未覆盖
│   └── ⚠️ Serde 序列化 (jsonplus/encrypted/msgpack) — 未覆盖
│
├── 三、执行与控制 ★★★★
│   ├── invoke/stream ★★★★
│   ├── interrupt/Command ★★★ (content_factory_hitl)
│   ├── Time Travel ★★★ (content_factory_timetravel)
│   ├── Update State ★★★
│   ├── ⚠️ astream/ainvoke/aget_state (异步) — 未覆盖
│   ├── ⚠️ bulk_update_state — 未覆盖
│   ├── ⚠️ NodeBuilder (细粒度节点控制) — 未覆盖
│   └── ⚠️ Pregel 类直接使用 — 未覆盖
│
├── 四、流式输出 ★★★
│   ├── stream_mode="values" ★★
│   ├── ⚠️ stream_mode="updates" — 未覆盖
│   ├── ⚠️ stream_mode="messages" (Token级) — 未覆盖
│   ├── ⚠️ stream_mode="custom" — 未覆盖
│   ├── ⚠️ stream_mode="debug" — 未覆盖
│   └── ⚠️ astream_events (标准事件) — 未覆盖
│
├── 五、容错与策略 ★ (几乎未覆盖)
│   ├── ⚠️ RetryPolicy — 未覆盖
│   ├── ⚠️ TimeoutPolicy — 未覆盖
│   ├── ⚠️ CachePolicy — 未覆盖
│   └── ⚠️ Node 级 retry/timeout/cache — 未覆盖
│
├── 六、长期记忆 (Store) ☆ (完全未覆盖)
│   ├── ⚠️ BaseStore — 未覆盖
│   ├── ⚠️ MemoryStore/PostgresStore — 未覆盖
│   ├── ⚠️ Embedding 搜索 — 未覆盖
│   └── ⚠️ 跨会话持久记忆 — 未覆盖
│
├── 七、Managed Values ☆ (完全未覆盖)
│   ├── ⚠️ ManagedValue 框架 — 未覆盖
│   └── ⚠️ is_last_step — 未覆盖
│
├── 八、预构建组件 (Prebuilt) ★ (部分覆盖)
│   ├── create_react_agent() ★ (类似 my_first_agent)
│   ├── ToolNode ★ (类似 tools_node)
│   ├── tools_condition ★
│   └── ⚠️ 深入了解 Prebuilt — 未覆盖
│
├── 九、可视化 ★ (未覆盖)
│   ├── ⚠️ get_graph() Mermaid — 未覆盖
│   ├── ⚠️ get_graph(xray=True) 子图展开 — 未覆盖
│   └── ⚠️ draw 图形输出 — 未覆盖
│
├── 十、远程与部署 ★★ (未覆盖)
│   ├── ⚠️ RemotePregel — 未覆盖
│   ├── ⚠️ SDK (sdk-py/sdk-js) — 未覆盖
│   ├── ⚠️ LangGraph Server — 未覆盖
│   └── ⚠️ CLI (langgraph dev/up/test) — 未覆盖
│
├── 十一、函数式 API ☆ (完全未覆盖)
│   ├── ⚠️ @entrypoint / @task — 未覆盖
│   └── ⚠️ 函数式 Agent 构建 — 未覆盖
│
└── 十二、测试 ★ (未覆盖)
    ├── ⚠️ LangGraph 测试工具 — 未覆盖
    └── ⚠️ Conformance 测试 — 未覆盖
```

**图例：**
- ★★★★★ = 核心必学 | ★★★★ = 重要 | ★★★ = 推荐 | ★★ = 了解 | ☆ = 进阶
- ⚠️ = test/ 中未覆盖，需补学

---

## 二、test/ 已覆盖内容 vs 完整功能对比

### 2.1 已覆盖（你的 test/ 目录 + 学习文档）

| 功能 | 对应文件 | 学习文档 |
|------|---------|---------|
| StateGraph 基础 | my_first_agent.py | 第一阶段 |
| Node/Edge/Conditional Edge | my_first_agent.py | 第一阶段 |
| Checkpointer (MemorySaver) | my_first_agent.py | 第一阶段 |
| Tool 绑定与调用 | my_first_agent.py | 第一阶段 |
| Supervisor 多Agent | multi_agent_team.py | 第二阶段 |
| 人工干预 interrupt | multi_agent_with_human.py | 第二阶段 |
| interrupt() 官方机制 | multi_agent_official.py | 第二阶段 |
| LLM 动态调度 | multi_agent_humantool.py | 第二阶段 |
| SqliteSaver 持久化 | multi_agent_sqlitesaver.py | 第二阶段 |
| Send 并行 | multi_agent_Send.py | 第三阶段 |
| Subgraph 子图 | multi_agent_Subgraphs.py | 第三阶段 |
| Stream 实时输出 | multi_agent_Stream.py | 第三阶段 |
| 4层子图嵌套 | multi_agent_Supervisor.py | 第三阶段 |
| Gradio Web界面 | content_factory_app.py | 第四阶段 |
| 多审核点 HITL | content_factory_hitl.py | 第四阶段 |
| 智能重写 | content_factory_write.py | 第四阶段 |
| Time Travel | content_factory_timetravel.py | 第四阶段 |
| AI 动态决策 | content_factory_ai_decision.py | 第四阶段 |
| 动态构图 | content_factory_dynamic.py | 第四阶段 |
| LangSmith 监控 | content_factory_langsmith.py | 第四阶段 |
| Web Search | content_factory_websearch.py | 第四阶段 |
| RAG 方案 | 笔记/19.RAG 增强.md | 第四阶段 |

### 2.2 未覆盖（需要补学）

| 模块 | 缺失功能 | 优先级 | 难度 |
|------|---------|--------|------|
| **StateGraph 高级** | input_schema/output_schema/context_schema | 🔴 高 | ⭐⭐ |
| **StateGraph 高级** | add_sequence() / set_finish_point() | 🟡 中 | ⭐ |
| **异步执行** | ainvoke / astream / aget_state | 🔴 高 | ⭐⭐ |
| **流式模式** | stream_mode=updates/messages/custom/debug | 🔴 高 | ⭐⭐ |
| **节点配置** | NodeBuilder (subscribe/read/write) | 🟡 中 | ⭐⭐⭐ |
| **容错策略** | RetryPolicy / TimeoutPolicy / CachePolicy | 🔴 高 | ⭐⭐ |
| **自定义 Reducer** | 自定义 State 合并逻辑 | 🟡 中 | ⭐⭐ |
| **Pydantic State** | 用 BaseModel 代替 TypedDict | 🟡 中 | ⭐ |
| **长期记忆 (Store)** | BaseStore / MemoryStore / 跨会话记忆 | 🔴 高 | ⭐⭐⭐ |
| **Managed Value** | managed values 框架 | 🟢 低 | ⭐⭐⭐ |
| **可视化** | get_graph() Mermaid 图 | 🟡 中 | ⭐ |
| **Remote** | RemotePregel / SDK | 🟡 中 | ⭐⭐⭐ |
| **函数式 API** | @entrypoint / @task | 🟢 低 | ⭐⭐ |
| **bulk_update_state** | 批量状态更新 | 🟢 低 | ⭐⭐ |
| **Channels 底层** | Topic / LastValue / NamedBarrierValue | 🟢 低 | ⭐⭐⭐⭐ |
| **Serde 序列化** | jsonplus / encrypted / msgpack | 🟢 低 | ⭐⭐⭐ |

---

## 三、补全学习路线

已经学过的部分按原计划继续。**新增的缺失功能**插入到对应阶段中：

### 第一阶段补全（插入第2-3周之间）

```diff
  第2周：状态管理与记忆
+  🔸 自定义 Reducer：不只是 add_messages
+     - 学习如何写自己的 Reducer 函数
+     - 场景：计数器、合并列表、自定义追加逻辑
+  🔸 Pydantic / DataClass State
+     - 对比 TypedDict / BaseModel / dataclass 三种 State 定义
+     - 什么时候用哪种
```

### 第二阶段补全（插入第5-6周之间）

```diff
  第5周：人工干预
+  🔸 stream_mode 全系列
+     - stream_mode="values"（已有）
+     - stream_mode="updates"（增量更新）
+     - stream_mode="messages"（Token 级流式）
+     - stream_mode="custom"（自定义事件）
+     - stream_mode="debug"（调试事件）
+     - astream_events() 标准事件流
```

### 第三阶段补全（插入第9-10周之间）

```diff
  第9周：子图嵌套
+  🔸 StateGraph 高级选项
+     - input_schema：只接收部分字段作为输入
+     - output_schema：只暴露部分字段作为输出
+     - context_schema：运行时上下文（与 State 分离）
+     - add_sequence()：快速定义串行节点序列
+  🔸 异步执行
+     - ainvoke() / astream() / aget_state()
+     - 异步节点函数（async def）
+     - 配合 FastAPI 做异步 Web 服务
```

### 第三阶段补全（插入第10周后）

```diff
  第10周：Stream 实时输出
+  🔸 可视化
+     - app.get_graph().draw_mermaid()
+     - app.get_graph(xray=True) 展开子图
+     - Mermaid 流程图自动生成
+  🔸 容错策略
+     - RetryPolicy：节点失败自动重试
+     - TimeoutPolicy：超时控制
+     - CachePolicy：缓存策略
+     - add_retry_policies() 节点级配置
```

### 第四阶段新增（插入第17周后）

```diff
  第17周：LangSmith 监控
+  🔸 长期记忆 (Store)
+     - BaseStore 接口
+     - InMemoryStore 内存存储
+     - PostgresStore 持久化
+     - 跨会话记忆（记住用户偏好）
+     - Embedding 相似搜索
+  🔸 Prebuilt 预构建组件
+     - create_react_agent() 官方方式
+     - ToolNode / tools_condition 源码解读
+     - 对比手写 Agent 和 Prebuilt Agent
```

### 第五阶段新增

```diff
  第五阶段：AI大神进阶
+  🔸 函数式 API
+     - @entrypoint / @task 装饰器
+     - 函数式 vs 图式 对比
+  🔸 Remote & SDK
+     - RemotePregel 远程图执行
+     - sdk-py 客户端
+     - LangGraph Server 部署
+  🔸 CLI 工具
+     - langgraph dev 开发服务器
+     - langgraph up 部署
+     - langgraph test 测试
+  🔸 Channels 底层
+     - Topic / LastValue / NamedBarrierValue
+     - 自定义 Channel
+  🔸 Serde 序列化
+     - jsonplus 格式
+     - Encrypted 加密序列化
+     - Msgpack 二进制格式
+  🔸 Managed Values
+     - is_last_step managed value
```

---

## 四、补充知识速查

### 4.1 input_schema / output_schema / context_schema

```python
class InternalState(TypedDict):
    """内部完整状态"""
    messages: Annotated[list, add_messages]
    draft: str
    api_key: str  # 敏感字段，不想暴露给外部

class InputState(TypedDict):
    """只接收 topic 和 messages 作为输入"""
    messages: Annotated[list, add_messages]
    topic: str

class OutputState(TypedDict):
    """只暴露文章正文"""
    draft: str

# 三态分离
graph = StateGraph(
    InternalState,      # 内部完整状态
    input=InputState,   # 输入只接收这些
    output=OutputState  # 输出只暴露这些
)
```

**场景：** API 文档中可以隐藏内部字段，用户只需要知道传入什么、拿到什么。

### 4.2 异步执行

```python
# 异步节点
async def async_node(state):
    # 可以并发调用多个 API
    import asyncio
    result1, result2 = await asyncio.gather(
        call_api_1(state["topic"]),
        call_api_2(state["topic"])
    )
    return {"combined": result1 + result2}

# 异步运行
config = {"configurable": {"thread_id": "001"}}
async for chunk in graph.astream(inputs, config):
    print(chunk)

# 或
result = await graph.ainvoke(inputs, config)
```

### 4.3 stream_mode 模式对比

```python
# values 模式：每次返回完整的 State
for chunk in app.stream(inputs, config, stream_mode="values"):
    print(chunk)  # 完整的 State 字典

# updates 模式：只返回变化的字段
for chunk in app.stream(inputs, config, stream_mode="updates"):
    print(chunk)  # {"researcher": {"research_notes": "..."}}

# messages 模式：Token 级别的流式输出
for chunk in app.stream(inputs, config, stream_mode="messages"):
    print(chunk)  # 单个 Token 或消息块

# custom 模式：节点主动 yield 的自定义事件
# debug 模式：超详细的执行事件
```

### 4.4 RetryPolicy / TimeoutPolicy / CachePolicy

```python
from langgraph.types import RetryPolicy, TimeoutPolicy, CachePolicy

# 重试策略
retry = RetryPolicy(
    max_attempts=3,          # 最多重试 3 次
    retry_on=Exception,      # 所有异常都重试
    backoff_factor=2.0,      # 退避因子（1s → 2s → 4s）
)

# 超时策略
timeout = TimeoutPolicy(
    timeout_seconds=30.0,    # 节点执行超时 30 秒
)

# 缓存策略
cache = CachePolicy(True)    # 相同输入直接返回缓存

# 应用到节点
builder.add_node("llm_call", my_function, 
    retry=retry,
    timeout=timeout,
)
# 或使用 NodeBuilder 链式调用
from langgraph.pregel import NodeBuilder
node = NodeBuilder(my_function)\
    .add_retry_policies(retry)\
    .set_timeout(timeout)\
    .add_cache_policy(cache)\
    .build()
```

### 4.5 Store（长期记忆）— 跨会话记忆

```python
from langgraph.store.memory import InMemoryStore

# Store 和 Checkpointer 的区别：
# Checkpointer = 当前对话的记忆（thread_id 范围内）
# Store = 跨对话的长期记忆（全局共享）

store = InMemoryStore()

# 编译时传入 store
app = graph.compile(checkpointer=memory, store=store)

# 在节点中读写 Store
def my_node(state, config, *, store):
    # 读取用户偏好
    user_prefs = store.get(("users", config["configurable"]["user_id"]), "prefs")
    
    # 写入长期记忆
    store.put(("users", config["configurable"]["user_id"]), "prefs", {
        "name": "张三",
        "preferred_style": "正式",
    })
    
    # 搜索（基于 Embedding）
    results = store.search(("documents",), query="AI 教育", limit=5)
```

### 4.6 可视化

```python
# 生成 Mermaid 流程图
mermaid_code = app.get_graph().draw_mermaid()
print(mermaid_code)

# 保存为图片（需要 Mermaid 支持）
# 带子图展开
mermaid_full = app.get_graph(xray=True).draw_mermaid()

# 直接输出为 ASCII 图
print(app.get_graph().draw_ascii())
```

### 4.7 add_sequence() — 快速序列

```python
# 传统方式（一个一个加）
builder.add_node("a", node_a)
builder.add_node("b", node_b)
builder.add_node("c", node_c)
builder.add_edge("a", "b")
builder.add_edge("b", "c")

# add_sequence 方式
builder.add_sequence(["a", "b", "c"])
# 一行定义串行链！
```

### 4.8 函数式 API

```python
from langgraph.func import entrypoint, task

@task
def research(topic: str) -> str:
    """调研任务"""
    return model.invoke([HumanMessage(content=f"调研{topic}")])

@task
def write_article(notes: str) -> str:
    """写作任务"""
    return model.invoke([HumanMessage(content=f"根据笔记写文章：{notes}")])

@entrypoint()
def content_workflow(topic: str):
    """函数式工作流"""
    notes = research(topic).result()     # 调用调研任务
    article = write_article(notes).result()  # 调用写作任务
    return article

# 运行
result = content_workflow.invoke("AI 教育", config)
```

---

## 五、优先补学建议

### 🔴 第一优先级（找工作必考）

| 功能 | 原因 | 建议学习时间 |
|------|------|-------------|
| `input_schema/output_schema` | 面试常问：如何控制输入输出 | 1 天 |
| `ainvoke/astream` 异步 | 现代 Web 应用标配 | 2 天 |
| `stream_mode` 全部模式 | 面试问得越来越细 | 2 天 |
| `RetryPolicy/TimeoutPolicy` | 工程稳定性必备 | 1 天 |
| **Store 长期记忆** | LangGraph v0.3+ 核心新功能 | 3 天 |
| **自定义 Reducer** | 理解 State 合并原理 | 1 天 |

### 🟡 第二优先级（提升工程能力）

| 功能 | 原因 | 建议学习时间 |
|------|------|-------------|
| 可视化 get_graph() | 调试和文档演示 | 0.5 天 |
| Pydantic State | 更严谨的状态定义 | 1 天 |
| Remote/SDK | 部署相关 | 2 天 |
| NodeBuilder | 细粒度控制 | 1 天 |

### 🟢 第三优先级（进阶了解）

| 功能 | 原因 | 建议学习时间 |
|------|------|-------------|
| Channels 底层 | 理解执行引擎 | 3 天 |
| 函数式 API | 另一种写法 | 1 天 |
| Serde 序列化 | 自定义存储 | 2 天 |
| Managed Values | 高级特性 | 1 天 |
| CLI 工具 | 部署运维 | 1 天 |

---

## 六、如何补学

### 方法 1：官方文档学习

LangGraph 官方文档覆盖所有功能，按以下路径查阅：

```
https://docs.langchain.com/oss/python/langgraph/
├── conceptual/           ← 概念理解
│   ├── state            ← State、Reducer、Schema
│   ├── streaming         ← 各种 stream_mode
│   ├── memory           ← Checkpointer vs Store
│   └── persistence      ← 持久化策略
├── how-tos/              ← 实操指南（每个功能都有）
│   ├── streaming         ← 流式示例
│   ├── subgraphs         ← 子图进阶
│   ├── store             ← 长期记忆
│   ├── retry             ← 重试策略
│   └── visualization     ← 可视化
└── reference/            ← API 参考
```

### 方法 2：从 test/ 扩展实验

每个新功能都可以基于你现有的 test/ 代码做实验：

```python
# 例如：在 content_factory_dynamic.py 基础上加 Store
# 让 Agent 记住用户的偏好设置

# 例如：在 multi_agent_Send.py 基础上加 RetryPolicy
# 让 LLM 调用失败时自动重试

# 例如：在 content_factory_app.py 基础上加 stream_mode
# 改成 Token 级流式输出
```

### 方法 3：对照源码看实现

```python
# 每个功能的源码入口
# libs/langgraph/langgraph/graph/state.py — StateGraph builder
# libs/langgraph/langgraph/pregel/main.py — Pregel runtime
# libs/langgraph/langgraph/types.py — RetryPolicy, TimeoutPolicy, Interrupt
# libs/checkpoint/langgraph/store/ — Store 实现
# libs/checkpoint/langgraph/checkpoint/ — Checkpointer 实现
```

---

## 七、更新后的完整学习路线

```
第一阶段（4周）: 基础入门
  ├── 第0周: Python + AI 基础
  ├── 第1周: 第一个 Agent（已覆盖）
  ├── 第2周: 状态管理 + 【补: 自定义Reducer + Pydantic State】
  └── 第3周: 条件路由（已覆盖）

第二阶段（5周）: 多Agent协作
  ├── 第4周: Supervisor 模式（已覆盖）
  ├── 第5周: 人工干预 + 【补: stream_mode 全系列】
  ├── 第6周: LLM 动态调度（已覆盖）
  └── 第7周: 持久化存储（已覆盖）

第三阶段（5周）: 企业级实战
  ├── 第8周: Send 并行（已覆盖）
  ├── 第9周: 子图 + 【补: input/output schema + 异步执行】
  ├── 第10周: Stream + 【补: 可视化 + 容错策略】
  └── 第11周: 内容工厂V1（已覆盖）

第四阶段（8周）: 产品化与面试
  ├── 第12-14周: HITL/TimeTravel/AI决策（已覆盖）
  ├── 第15周: 动态构图（已覆盖）
  ├── 第16周: LangSmith（已覆盖）
  ├── 第17周: WebSearch/RAG（已覆盖）
  ├── 第18周: 【补: Store 长期记忆 + Prebuilt 组件】
  └── 第19周: 面试准备（已覆盖）

第五阶段（持续）: AI大神进阶
  ├── 【补: 函数式API + Remote/SDK + CLI】
  ├── 【补: Channels 底层 + Serde + Managed Values】
  ├── 源码深入研究
  └── 前沿方向探索
```

---

> **总结：** test/ 覆盖了 LangGraph 约 **60%** 的核心功能。剩下的 **40%** 包括：异步、流式模式、容错、Store、可视化、高级 Schema、函数式 API、SDK/Remote 等。
>
> 📎 **关联文档：** `笔记未完成分支汇总.md` — 笔记中提到的方向哪些做了、哪些没做（13个已完成 vs 13个未完成）
>
> 建议按"第一优先级"先补最面试常考的 6 个功能，其他的随用随学即可。
