# 第一阶段：基础入门 — 从零到第一个 Agent

> 🕐 **时间：** 第0-3周（建议每天1-2小时）
> 🎯 **目标：** 理解 StateGraph、Node、Edge、State 四大核心概念，能独立构建带记忆的 Agent
> 📁 **对应代码：** `test/my_first_agent.py`

---

## 第0周：Python + AI 基础扫盲

### 📚 你需要提前知道的事

LangGraph 本质上是一个 Python 库。你不需要成为 Python 高手，但以下语法必须熟悉：

### Python 必学语法

```python
# 1. 类型注解（Type Hint）— LangGraph 大量使用
def greet(name: str) -> str:          # → 表示返回值类型
    return f"Hello, {name}"

from typing import List, Dict
scores: List[int] = [85, 92, 78]      # 列表类型注解
user: Dict[str, any] = {"name": "张三"} # 字典类型注解

# 2. TypedDict — LangGraph 状态定义的核心
from typing import TypedDict

class MyState(TypedDict):
    messages: list       # 消息列表
    topic: str           # 主题
    result: str          # 结果

# 3. Annotated — 给类型加"元数据"
from typing import Annotated
from langgraph.graph.message import add_messages

# 意思是：这是一个 list，追加新消息时用 add_messages 函数
messages: Annotated[list, add_messages]

# 4. 函数作为参数传递 — LangGraph 节点就是函数
def my_node(state):
    return {"result": "done"}

builder.add_node("node_name", my_node)  # 把函数当参数传进去
```

### 🤖 什么是 LLM（大语言模型）？

**一句话：** LLM 是一个"超级智能文本补全器"。

```
你输入: "中国的首都是"
LLM 输出: "北京"

你输入: "北京的天气怎么样？用工具查一下"
LLM 输出: "好的，我来查一下..." + tool_calls
```

LLM 的特点：
- **不是计算器** — 它不"算"数学，它"猜"答案
- **有知识但会编** — 它记得很多知识，但不知道的会"瞎编"（这叫**幻觉**）
- **需要工具** — 查天气、算数学这些精确的事，应该让它调用工具

### 🔧 ChatOpenAI 使用

LangGraph 用 `ChatOpenAI` 来调用 LLM。

```python
from langchain_openai import ChatOpenAI
import os

# 初始化模型
model = ChatOpenAI(
    model="glm-4-flash",              # 模型名（智谱 GLM）
    openai_api_base="https://open.bigmodel.cn/api/paas/v4/",  # API 地址
    api_key=os.getenv("ZHIPU_API_KEY"), # API Key（从环境变量读取）
    temperature=0.7                    # 创造性（0=确定，1=随机）
)

# ⭐ 核心用法：invoke（调用）
response = model.invoke([HumanMessage(content="你好！")])
print(response.content)  # 打印 LLM 回复

# ⭐ 工具绑定：告诉 LLM 它能用哪些工具
@tool
def get_weather(city: str) -> str:
    """查询指定城市的天气"""
    return f"{city}今天25度"

model = model.bind_tools([get_weather])  # 绑定工具
```

### 💡 第0周任务

1. 打开 `test/my_first_agent.py`，**运行它**（你应该已经成功了）
2. 把 `get_weather` 里的假数据改成真实的城市名和温度
3. 加一个 `calculator` 工具
4. 问 LLM 一个数学题，观察它是怎么调用工具的

---

## 第1周：第一个 Agent — ReAct 模式

### 📖 核心理解

**Agent = LLM + 工具 + 循环决策**

```
        用户提问
           │
           ▼
    ┌──────────────┐
    │  LLM 思考     │  ← 决定：直接回答还是调用工具？
    └──────┬───────┘
           │
     ┌─────┴───────┐
     ▼              ▼
  直接回答        调用工具
                    │
                    ▼
              工具返回结果
                    │
                    ▼
              ┌──────────┐
              │ LLM 再次思考│  ← 看工具结果，决定下一步
              └──────────┘
                    │
              ┌─────┴──────┐
              ▼              ▼
           直接回答        再调工具 （循环直到可以直接回答）
```

这个模式叫 **ReAct（Reasoning + Acting，推理 + 行动）**。

### 🧩 my_first_agent.py 逐行拆解

```python
# ===== 1. 定义状态（State）=====
class State(TypedDict):
    """状态：就是节点之间的"共享白板" """
    messages: Annotated[list, add_messages]
    # messages = 对话历史
    # Annotated[list, add_messages] = "这是一个列表，追加新消息时用 add_messages 函数"

# ===== 2. 定义工具（Tools）=====
@tool
def get_weather(city: str) -> str:
    """查询指定城市的天气"""  # ← 这个描述 LLM 会读到！
    return f"{city}今天25度，晴"
    # @tool 装饰器：把普通函数变成"LLM能调用的工具"
    # city: str → LLM 知道要传城市名
    # 函数描述 → LLM 知道这个工具是干嘛的

# ===== 3. 绑定工具到模型 =====
model = ChatOpenAI(model="glm-4-flash", temperature=0)
model = model.bind_tools([get_weather])
# bind_tools：告诉 LLM "你有这些工具可以用"

# ===== 4. 定义节点（Node）=====
def chatbot(state: State):
    """聊天节点：LLM 根据当前状态决定下一步"""
    # 输入：当前状态（包含历史消息）
    # 输出：状态更新（新消息）
    response = model.invoke(state["messages"])
    return {"messages": [response]}
    # 关键：如果 LLM 决定调用工具，response.tool_calls 会有内容
    # 如果 LLM 决定直接回答，response.content 就是回答文本

def tools_node(state: State):
    """工具节点：执行 LLM 要求的所有工具调用"""
    outputs = []
    for tool_call in state["messages"][-1].tool_calls:
        # 解析 LLM 要调用哪个工具、传什么参数
        tool_name = tool_call["name"]
        tool_args = tool_call["args"]
        
        # 执行对应的工具函数
        if tool_name == "get_weather":
            result = get_weather.invoke(tool_args)
        
        outputs.append(ToolMessage(content=str(result), tool_call_id=tool_call["id"]))
    
    return {"messages": outputs}

# ===== 5. 构建图（Graph）=====
builder = StateGraph(State)

# 加节点
builder.add_node("chatbot", chatbot)
builder.add_node("tools", tools_node)

# 加边
builder.add_edge(START, "chatbot")          # 入口 → chatbot
builder.add_conditional_edges(
    "chatbot",
    tools_condition,  # 判断：LLM 要调工具？→ 走去 tools；否则走到 END
)
builder.add_edge("tools", "chatbot")        # 工具执行完 → 回到 chatbot

# 编译
app = builder.compile()

# ===== 6. 运行 =====
config = {"configurable": {"thread_id": "001"}}
inputs = {"messages": [("user", "北京天气怎么样？")]}

for chunk in app.stream(inputs, config):
    print(chunk)  # 每次执行一个节点就打印
```

### 🔄 ReAct 循环可视化

```
Step 1: chatbot 运行
  → LLM 说："我需要查天气"
  → response.tool_calls = [{"name": "get_weather", "args": {"city": "北京"}}]
  → response.content = ""（空，因为还没决定怎么回答）

Step 2: tools 运行（因为 tools_condition 检测到 tool_calls）
  → 执行 get_weather(city="北京")
  → 返回 ToolMessage(content="北京今天25度，晴")

Step 3: chatbot 再次运行（因为 tools → chatbot 是普通边）
  → LLM 看到工具结果
  → response.content = "北京今天25度，晴天"
  → response.tool_calls = None（没有要调的工具了）

Step 4: END（因为 tools_condition 检测到没有 tool_calls）
  → 流程结束
```

### 💡 第1周任务

1. **理解每一个函数的作用** — 把 `my_first_agent.py` 的注释读三遍
2. **加 DEBUG 日志** — 在 `chatbot` 函数里加 `print(f"tool_calls: {response.tool_calls}")`，观察 LLM 的"思考过程"
3. **加 calculator 工具** — 加一个计算器工具，问数学题测试
4. **不看代码，自己写一遍** — 这是检验是否真理解的最好方法

---

## 第2周：状态管理与记忆

### 🤔 为什么需要记忆？

```python
# 没有记忆的情况
你问："北京的天气怎么样？"    → Agent 回答了
你再问："那上海的呢？"        → Agent 不知道你在说天气！
```

**原因：** 每次调用 `app.stream()` 都是独立运行，上一轮的 State 丢掉了。

### 💾 Checkpointer = 存档系统

```python
from langgraph.checkpoint.memory import MemorySaver

# 1. 创建存档
memory = MemorySaver()

# 2. 编译时传入
app = workflow.compile(checkpointer=memory)

# 3. 运行时告诉它是"谁"的对话
config = {"configurable": {"thread_id": "user_001"}}

# 第一次
inputs = {"messages": [("user", "北京的天气？")]}
for chunk in app.stream(inputs, config):
    pass

# 第二次（用同一个 thread_id）
inputs2 = {"messages": [("user", "那上海的呢？")]}
for chunk in app.stream(inputs2, config):  
    # ✨ Agent 会自动从 memory 读取历史消息
    # 不需要你手动传历史！
    pass
```

### 🧠 记忆原理

```
第一次对话：
  输入 → [chatbot] → [tools] → [chatbot] → 输出
           │                          │
           ▼                          ▼
      Checkpointer 保存 step1     Checkpointer 保存 step2

第二次对话：
  输入："那上海的呢？"
         │
         ▼
  Checkpointer 读取历史 → "哦，之前聊的是天气"
         │
         ▼
  带上历史消息 → [chatbot] → [tools] → [chatbot] → 正确回答上海天气
```

### ⚠️ 一个重要理解

Agent **记得历史**，但不会**自动延续任务**。

```python
# 第一轮
"帮我查北京天气，如果打八折是多少"
# Agent 查天气(25度) → 计算(25*0.8) → 回答20度

# 第二轮："那上海的呢？"
# Agent 只查了上海天气 → 回答
# 它没有计算打折！因为"打八折"是上一轮的任务，不是这轮的
```

不是记忆丢失，是 Agent 的**逻辑判断**——它认为新的一轮对话应该重新理解意图。

### 💡 第2周任务

1. 给 `my_first_agent.py` 加上 MemorySaver
2. 测试记忆：先问"北京的天气"，再问"那上海的呢？"
3. 测试任务延续：先问"查天气并打八折"，再问"那上海的呢？"——观察区别
4. **理解**：为什么第二轮不算打折？这是 Bug 还是预期行为？

---

## 第3周：条件路由与分支

### 🤔 为什么需要条件路由？

一个 Agent 不可能把所有情况写死在固定路径里：

```python
# 固定路径（不够灵活）
chatbot → tools → chatbot → END
# 不管要不要调工具，都走这个路径

# 条件路径（灵活）
chatbot → [需要工具? → tools → chatbot] [直接回答? → END]
```

### 🔀 条件边（Conditional Edge）

```python
def should_continue(state: State) -> str:
    """路由函数：根据当前状态决定下一步"""
    messages = state["messages"]
    last_message = messages[-1]
    
    # 如果 LLM 要求调工具 → 走 tools 节点
    if last_message.tool_calls:
        return "tools"
    
    # 如果 LLM 直接回答 → 结束
    return "end"

# 使用条件边
workflow.add_conditional_edges(
    "chatbot",           # 从哪个节点出发
    should_continue,     # 路由函数（返回节点名）
    {                    # 映射：返回值 → 实际节点
        "tools": "tools",
        "end": END,
    }
)
```

### 🎯 路由函数的三种写法

```python
# 写法1：独立函数（推荐，逻辑复杂时用）
def router(state):
    if state.get("research_notes") is None:
        return "researcher"
    elif state.get("draft") is None:
        return "writer"
    return "FINISH"

# 写法2：lambda（适合简单判断）
lambda state: "tools" if state["messages"][-1].tool_calls else END

# 写法3：从 State 读取 next_agent（最灵活，让节点自己决定下一步）
def supervisor_node(state):
    # ... 复杂逻辑 ...
    return {"next_agent": "researcher"}  # 节点直接告诉路由下一步

# 路由函数就变成：
lambda state: state["next_agent"]
```

### 💡 第3周任务

1. 在 `my_first_agent.py` 中，找到条件边的代码，理解它的判断逻辑
2. 尝试修改路由逻辑：让 Agent 先调工具，但限制最多调 3 次工具就强制结束
3. 把 `multi_agent_team.py` 跑通，理解 Supervisor 的路由逻辑

---

## 🏆 里程碑考核：构建带记忆的天气 + 计算器 Agent

### 要求

```python
# 你的 Agent 需要支持：
# 1. 查天气
# 2. 数学计算
# 3. 多轮对话记忆
# 4. 条件路由

# 测试对话：
用户: "北京的天气怎么样？"
Agent: "北京今天25度"

用户: "那上海的呢？"  
Agent: "上海今天28度"  # ← 记得在聊天气！

用户: "25乘以8等于多少？"
Agent: "25乘以8等于200"  # ← 调用了计算器

用户: "刚才那个数加10呢？"
Agent: "200加10等于210"  # ← 记得刚才算的结果！
```

### 验收清单

| 能力 | 通过标准 |
|------|---------|
| 工具调用 | Agent 能正确调用 get_weather 和 calculator |
| 多轮记忆 | 第二次提问能理解上下文 |
| 条件路由 | 需要工具时走 tools，不需要时直接回答 |
| 代码理解 | 能解释每行代码的作用 |

### 如果你卡住了...

1. 重新跑一遍 `test/my_first_agent.py`
2. 打开 `test/笔记/1.my_first_agent..md` 看笔记
3. 先别追求完美，跑通了就是胜利
4. **最重要的是理解流程** — 画一张图，标出数据流动的方向

---

## 🚀 第一阶段总结

### 你学到的核心概念

```
✅ StateGraph       — 画工作流图的画板
✅ State            — 节点间共享的"白板"
✅ Node             — 图里的一个工作步骤
✅ Edge             — 步骤之间的连线
✅ Conditional Edge — 根据状态动态决定走向
✅ Compile          — 把图变成可执行的程序
✅ Checkpointer     — Agent 的记忆
✅ ReAct 模式       — 推理 + 行动的循环
```

### 你做过的代码实验

| 实验 | 学到了什么 |
|------|-----------|
| 加 DEBUG 日志 | LLM 的"思考过程"是怎么样的 |
| 加 calculator 工具 | 怎么给 Agent 加新能力 |
| 加 MemorySaver | 怎么让 Agent 记住对话 |
| 改条件路由 | 怎么控制流程走向 |
| 自己重写代码 | 是否真的理解了 |

### 下一步

你已经掌握了 LangGraph 的基础。从第二阶段开始，你将学会让**多个 AI Agent 像一个团队一样协作**。

> 📖 打开 `第二阶段-多Agent协作.md` 继续学习
